import DesignDrawingsPage from '../../../components/DesignDrawingsPage'
export default function ContractDrawings() {
  return <DesignDrawingsPage pageKey="contract-drawings" set="contract" title="Contract Drawings"
    intro="Architect contract drawings. Drawing information is read from the PDF title block on upload - check and correct as needed." />
}
