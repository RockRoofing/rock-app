Application PDF — fix summary overflow + add table gridlines
============================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single-file change (lib/applicationPdf.js).

1) SUMMARY / CERTIFICATE NO LONGER RUN OFF THE PAGE
   The value columns were positioned at fixed offsets that pushed the third column
   ~63pt past the right margin (why "£319,32…" / "£377,67…" were cut off). Rebuilt
   the geometry as three right-aligned money columns that all sit inside the page
   width. Verified the widest figure (£319,329.66) fits with room to spare and no
   column right-edge exceeds the page margin.

2) GRIDLINES ON ALL TABLES
   • Vertical column separators in BLACK.
   • Horizontal row rules in LIGHT GREY.
   Applied to Summary, Payment Certificate, Contract Works, Variations and Materials
   on Site. Verticals are drawn per row so they stay correct across page breaks.

FILES
-----
• lib/applicationPdf.js
