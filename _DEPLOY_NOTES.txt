DEPLOY NOTES - Cash Flow: Total in/out per week + frozen totals rows
====================================================================

ONE file, an EDIT - just replace it.
  pages/project-cashflow.js

CHANGES
1. TOTAL IN / TOTAL OUT rows added:
     - "Total IN / week"  = Sales in + Retention in   (green, bold)
     - "Total OUT / week" = Labour out + Materials out (red, bold)
   Shown per column alongside the individual stream rows, in both day and week views.

2. FROZEN ROWS: the header and all the totals rows (Sales in, Retention in, Total IN,
   Labour out, Materials out, Total OUT) now STICK to the top and stay visible when you
   scroll down through the project list. The Project + date columns remain frozen on
   the left, so the totals grid is always in view.

The gantt now scrolls vertically within the panel (height fits the window) so the
frozen rows have something to stick against.

BUILD: next build -> Compiled successfully.
