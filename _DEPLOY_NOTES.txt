pkg714 - FIX: "td is not defined", and full width
==================================================

*** SUPERSEDES pkg591 - pkg713. ***
*** SEVEN NEW FILES - create these in GitHub (as before) ***
*** lib/cashflowEvents.js IS IN THIS PACKAGE - replace it ***


CHANGED FILES (replace)
-----------------------
  pages/business-financials/monthly.js
  pages/business-financials/forecast-pl.js
  ... and the rest of the set as before


THE ERROR - MY BUG, AND THE SAME SHAPE AS EVER
------------------------------------------------
I put the Drill component at MODULE SCOPE, which is right - a component declared
inside another remounts on every render. But `td` and `th` were declared INSIDE
the page component, so Drill could not see them.

Compiles clean. Throws "td is not defined" the moment the table renders.

Both style objects hoisted to module scope, above the components that use them.

I also ran a scan for any other module-scope component reaching into page scope on
both pages - clean. The three it flagged in DrillTable were map callback
parameters, `(c, i)` and `(x, i)`, declared where they are used.


FULL WIDTH
----------
    12-MONTH   was maxWidth 1300 with sixteen columns, so it scrolled sideways on
               every screen including ones with room to spare. Now full width, and
               the table minWidth raised to 1500 so the columns are not crushed.

    FORECAST P&L   the table had no width set, so it sized to its content and left
               a third of a wide monitor empty. Now width 100%.
