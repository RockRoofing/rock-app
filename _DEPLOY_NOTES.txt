DEPLOY NOTES - Design Portal PHASE 2: Permissions matrix + horizontal home cards
================================================================================

TWO files, both EDITS - just replace them.
  pages/admin.js    (new Permissions matrix section)
  pages/index.js    (portal home cards now flow horizontally)

WHAT THIS DOES
1. PERMISSIONS MATRIX (Admin > Portal Users, below the user list)
   A read-only grid: rows = portal areas (Pre-Contract, Commercial, Operations,
   Design, Lessons Learnt, HR, Bookkeeping, Management, Admin, Business Financials),
   columns = access levels (Pre-Contract, Post-Contract, Accounts, Designer,
   Management, Admin). A green tick shows which level can access each area. This makes
   the new Designer access (Design only) visible at a glance.

2. HOME CARDS HORIZONTAL
   The portal home department cards now lay out horizontally across the page (a
   responsive row that wraps), instead of the old narrow 2-wide column.

BUILD: next build -> Compiled successfully.
