pkg401 - Portal link in the CRM toolbar
=======================================

Deploy after pkg400. One changed file: pages/crm.js. No new files.

The CRM toolbar now reads, left to right:

    [logo]  [ CRM | Pre-Contract ]  <- Portal  |  Saved  |  Pipeline/List  ...

Same position relative to the toggle as on the Pre-Contract nav, and styled to match that
one - grey 13px with the divider after it - rather than the CRM's own bordered buttons.
It is the same control doing the same job, so it should not look like a different one.
