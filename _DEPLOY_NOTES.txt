Bookkeeping — DIAGNOSTIC v2 (find where the wage date lives)
===========================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully

Wages still have no date and sync is slow -> the ManualJournals list is omitting
lines AND date for this tenant, and the by-ID date isn't landing either. Rather
than guess the field shape, this enriches ?debug=1 to show the RAW date fields and
object keys on both the list entry and the by-ID detail.

RUN THIS (writes nothing):
    https://app.rockroofing.co.uk/api/sync-wages?debug=1

Paste the whole JSON back. It now shows, for the list sample and the detail:
  - Date_raw / DateString_raw  (exact date values, or null)
  - topLevelKeys               (every field name present)
  - firstLineKeys              (line-level field names)
Once I can see where the date actually is, I'll fix the parse + speed for good.

FILE CHANGED
------------
• pages/api/sync-wages.js — richer debug output (no behaviour change to real sync)
