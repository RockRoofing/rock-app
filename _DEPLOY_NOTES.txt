DEPLOY NOTES - Sales: fix signs (sales +, credit notes -), remove "now" line
============================================================================

SIGN FIX
--------
Your WIP manual journals post the sale to the sales code as a POSITIVE amount, but the
code was flipping it - so sales showed as MINUS. Fixed: manual-journal sales now show
POSITIVE. Result:
  - Invoices        -> positive (sales)
  - Manual journals -> positive (WIP sales)
  - Credit notes    -> negative (reductions)
So sales read as plus and credit notes as minus, as expected. (Totals are unchanged in
magnitude - they already matched - this only corrects the display sign.)

CHART
-----
Removed the green "now" vertical line from the chart and its "Now" entry from the key.
The key now shows just Target and Average.

DEPLOY
------
Replace both files. Commit -> Vercel "Ready" -> hard-refresh Sales -> Sync Xero figures
(to re-pull with the corrected sign).

VERIFY
------
- Sales lines (Invoice / Manual journal) show as positive; credit notes negative.
- No green "now" line on the chart; key shows Target + Average only.
- Month totals unchanged from before (only signs corrected).

FILES (both EDITS - no new files)
---------------------------------
  - lib/xero.js                          (manual-journal sign: sales positive)
  - pages/business-financials/sales.js   (remove now line + key entry)

BUILD
-----
Validated with `next build` -> "Compiled successfully".
