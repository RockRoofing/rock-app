pkg644 - FIX: focusKey is not defined (and a second one behind it)
===================================================================

*** SUPERSEDES pkg591 - pkg643. ***
*** THREE NEW FILES - create these in GitHub:
      pages/business-financials/forecast-margin.js
      pages/business-financials/balance-sheet.js
      components/PageErrorBoundary.js
***


CHANGED FILES (replace)
-----------------------
  pages/project-cashflow.js               <- the fix
  components/BizNav.js
  pages/_app.js
  pages/api/business-financials.js
  pages/business-financials.js
  pages/business-financials/cashflow.js
  pages/business-financials/invoice-finance.js
  pages/api/dashboard.js
  pages/connect.js
  lib/xero.js


THE BOUNDARY PAID FOR ITSELF IMMEDIATELY
----------------------------------------
"focusKey is not defined" - one round trip instead of five.

Row() is a MODULE-SCOPE component, so it cannot see the page component's state. I
used focusKey and setFocusKey inside it when I made the forecast bars clickable.
They are now passed as props.

AND A SECOND ONE SITTING RIGHT BEHIND IT. The same three lines also used `pk`,
which is the parent loop's variable and does not exist in Row either. It would
have thrown the instant focusKey was fixed - another round trip. Row already
receives `p`, so p.key is the same value.


I THEN SCANNED EVERY COMPONENT IN BOTH CASH FLOW FILES
------------------------------------------------------
Stripping comments and strings, and accounting for destructured state and
callback parameters, for identifiers that resolve to no parameter, local, module
declaration or import.

  project-cashflow.js   Row, HypAppModal, SpreadRow, PositionPanel   clean
  cashflow.js           DateCell, NetPositionBox                     clean
  forecast-margin.js                                                 clean
  balance-sheet.js                                                   clean

This is the third time this exact fault has shipped - a module-scope component
reaching for the page's state. It compiles perfectly every time, which is why the
build never catches it. The scan is now part of what I run before packaging.


ALSO IN THIS PACKAGE
--------------------
pkg643 - FORECAST MARGIN tab. Accrual basis: revenue by the month each sales line
belongs to, materials when DELIVERED, labour when the work was DONE. Says so
outright when the margin is not credible for roofing. Gross margin only -
overheads are the next step.

pkg642 - the error boundary itself. Worth keeping permanently.
