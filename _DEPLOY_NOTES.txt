DEPLOY NOTES - Planning: unlimited historic back-scroll
=======================================================

ONE file, an EDIT - just replace it.
  pages/operations/project-management/planning.js

WHAT CHANGED
  Historic mode previously stopped scrolling back at a ~6-week floor, so older
  programmed / actual works couldn't be viewed. Now:
    - "Historic" still OPENS 2 weeks before today (unchanged default).
    - You can scroll BACK with no limit (the "<" back button is never disabled) to
      view any previously programmed or actual works, however far back.

  Nothing else changes - forward horizon, editing, and the clash/planned-start
  features are all as before.

BUILD: next build -> Compiled successfully.
