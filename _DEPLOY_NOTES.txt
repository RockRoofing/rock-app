OPS FULL-WIDTH TABLES + PORTAL RENAME

  components/OperationsShell.js  — 'wide' Ops pages are now full browser width
                                   (was capped at 1800px). This gives ALL Ops table
                                   pages (Procurement, Deliveries, Tasks, Risks,
                                   Procurement Savings, Project Financials, Variations)
                                   the same room as the Commercial/Project Financials
                                   page, so they stop scrolling too much.
  pages/index.js                 — main portal entry "Project Financials" renamed to
                                   "Commercial" (page titles inside stay as they were).

NOTE: Procurement already has a zoom control. Deliveries/Procurement Savings use fixed
minWidths so they scroll when narrower than content — now that the page is full width,
that scrolling is greatly reduced. If you want the exact Commercial zoom behaviour on a
specific table, say which and I'll match it.
