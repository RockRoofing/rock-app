DEPLOY NOTES - Customer RFI attachments + big Tech Sub Approve
==============================================================

THREE files, all EDITS - just replace them.
  pages/api/design-rfis.js                 (allow attachments to be added by anyone)
  pages/design/[project]/rfis.js           (Add attachments button in the open RFI window)
  pages/design/[project]/tech-sub.js       (large, clear Approve button)

CHANGES
1. RFI - ADD ATTACHMENTS: the open RFI window now has a clear purple
   "[paperclip] Add attachments" button. Customers (and Rock Roofing) can upload one or more
   files straight onto the RFI - no need to be in edit mode. Uploaded files appear in the
   attachments viewer as normal.

2. TECH SUB - APPROVE BUTTON is now large and clear for the assigned customer approver:
   - In the table: a bigger green "[tick] Approve" button on the row.
   - In the open Tech Sub: a big "[tick] Approve Tech Sub" button in the header AND a
     prominent green approve bar at the top of the window ("Ready to approve this Tech Sub?").
   (Only the assigned customer sees Approve; Rock Roofing still can't self-approve.)

BUILD: next build -> Compiled successfully.
