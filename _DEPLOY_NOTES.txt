DEPLOY NOTES - Fix "Could not build zip" on Handover Docs
=========================================================

THREE files, all EDITS - just replace them.
  pages/api/design-handover-zip.js       (rewritten - no external library)
  pages/design/[project]/handover-docs.js (clearer error message)
  package.json                            (REMOVES the "archiver" dependency)

WHAT WAS WRONG
  The zip download used the "archiver" library. That dependency wasn't reliably installed/
  bundled on the server, so the zip endpoint errored and the page just said "Could not
  build zip".

THE FIX
  The zip is now built WITHOUT any external library - it uses only Node's built-in tools to
  write a standard .zip. This removes the dependency entirely (no install step, nothing to
  go wrong on deploy). Verified the output opens cleanly as a normal zip.

  Because of this, package.json no longer needs "archiver" - this package removes that line.
  If you already committed the earlier package.json with "archiver", just replace it with
  this one (removing archiver is harmless and avoids the failed install).

BUILD: next build -> Compiled successfully.
