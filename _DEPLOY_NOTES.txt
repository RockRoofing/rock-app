PLANNING — WLA email + PDF colour-coding

  pages/api/planning-week-email.js
   - Subject changed to "Rock Roofing Project Allocation".
   - Colour KEY added directly below the intro line (above the dates): green = Confirmed, blue = Provisional.
   - Each allocation is coloured by status (green confirmed / blue provisional). NO "— provisional" or
     "— confirmed" text after the project name — the colour + key convey it. (The in-body heading
     "Your upcoming project allocation" is unchanged, as requested.)

  lib/weeklyLabourPdf.js
   - "Week commencing …" moved down so it isn't crowding the logo.
   - Colour key (Confirmed green / Provisional blue) added under the date line.
   - Allocation entries coloured by status: green = confirmed, blue = provisional, orange = unnamed/TBC.
   (Multi-week one-page-each + taller rows from the previous drop are retained.)

Deploy over previous. No env / cron change.
