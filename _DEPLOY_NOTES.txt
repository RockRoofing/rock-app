DEPLOY NOTES - Confirm the build is live (v3 marker) - do NOT sync
==================================================================

IMPORTANT: You do NOT need to sync invoices. The diagnostic already showed 93
invoices qualify with paid dates present - the data is fine. The card you saw was
still running the OLD code (it lacked the "Avg = N days" text the fix adds), which
means the previous deploy didn't take effect in the browser/build.

WHAT THIS BUILD ADDS
--------------------
A visible version marker: the card is now titled
   "Average Days Beyond Terms (v3)"
So you can confirm at a glance which build is live. If after deploying + hard-refresh
the title does NOT say "(v3)", the deploy/caching is the problem (not the data).

It also (re)includes the display fix from the last build:
  - API returns paymentDiag.overallAvgDaysBeyondTerms (one authoritative average).
  - Headline reads that value directly.
  - Diagnostic line prints "... 93 qualify. Avg = N days beyond terms."

FILES (both EDITS - no new files). BOTH must go up together:
  - pages/api/commercial-metrics.js
  - pages/commercial-scorecard.js

DEPLOY
------
1. Replace BOTH files in GitHub. Commit.
2. In the Vercel dashboard, wait until the new deployment shows "Ready".
3. HARD-refresh the Scorecard (Ctrl/Cmd + Shift + R), or open in a private window.

THEN CHECK
----------
  - Card title should read "Average Days Beyond Terms (v3)". If it doesn't, the new
    build isn't being served - check Vercel shows the latest commit as Ready, and
    that BOTH files were committed. Syncing will NOT fix a stale build.
  - Once (v3) shows: the grey line should read "... 93 qualify. Avg = N days beyond
    terms." Tell me that N, or click the card and screenshot a few rows of the
    per-invoice Due Date / Paid Date / Days +/- Terms.

WHY "ON TERMS" MIGHT BE REAL (or not)
-------------------------------------
93 invoices averaging ~0 days is possible but worth checking. Once (v3) is live and
we can see "Avg = N", if the per-invoice column shows a realistic spread (some +20,
some -5) the average is genuine. If many rows show exactly 0, that points to a
dueDate/terms data issue in Xero (e.g. due date = invoice date) and I'll chase it.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
