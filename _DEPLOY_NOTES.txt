DELIVERIES — Supplier field in Add/Edit form

  pages/operations/project-management/deliveries.js
   - The Add/Edit delivery form now has a "Supplier" text box (below PO number / Order date), which
     populates the Supplier column in the table. Previously the Supplier column could only be filled by the
     Xero sync — manually added deliveries had no way to set it.
   - "+ Add delivery" now initialises a supplier field too.

Deploy this single file. No cron/env change.
