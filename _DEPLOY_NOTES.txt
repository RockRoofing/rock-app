DEPLOY NOTES - Remove Leak Test Cert Builder page
=================================================

TWO files, both EDITS - just replace them.
  lib/designShell.js                              (removes it from the Design nav)
  pages/design/[project]/leak-test-builder.js     (old URL now redirects to Leak Test Certs)

WHAT CHANGED
  - "Leak Test Cert Builder" is removed from the Design portal navigation.
  - If anyone still has an old link to it, that page now just redirects to "Leak Test Certs".
  - (The file is kept as a small redirect rather than deleted, so there's nothing to delete
    in GitHub - just replace it. If you'd rather fully delete the file, you can remove
    pages/design/[project]/leak-test-builder.js in GitHub and it'll 404 instead.)

BUILD: next build -> Compiled successfully.
