DEPLOY NOTES - Cash Flow: daily in/out totals + retention releases
==================================================================

ONE file, an EDIT - just replace it.
  pages/project-cashflow.js

1. DAILY CASH TOTALS ON THE GANTT (day view)
   Two new rows under the date header:
     - "Total in / day"  (green)  - sales received + retention released that day
     - "Total out / day" (red)    - labour instalments + materials payments that day
   Values only show on days where cash actually moves; hover shows the exact figure.
   (Day view only - week view omits them to stay readable.)

2. RETENTION RELEASES (cash IN)
   Retention is released on the RETENTION TRACKER's release dates (release 1 & 2),
   so timing has a single source of truth and can't duplicate. The AMOUNT is
   recomputed on the total ANTICIPATED FINAL ACCOUNT (finalAccount, else project
   value) x retention %, split 50/50 across the two dates - NOT the invoiced value.
   Releases already marked received are skipped.

NOTE: cash in = sales + retention releases; cash out = labour + materials. Uses the
payment terms/dates from each saved forecast. Build more forecasts to populate the
totals.

BUILD: next build -> Compiled successfully.
