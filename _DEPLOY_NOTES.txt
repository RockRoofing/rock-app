DEPLOY NOTES - Planner: freeze date header + totals row when scrolling down
===========================================================================

ONE file, an EDIT - just replace it.
  pages/operations/project-management/planning.js

WHAT CHANGED
  The date/week header row and the "Total installers" row now STICK to the top of the
  planner as you scroll down through the projects. Previously, scrolling to the lower
  projects hid the dates so you couldn't tell which day you were allocating to - now they
  stay pinned at the top.

  - The planner table now scrolls inside its own box (capped to the window height) with
    both the top header rows frozen. The left-hand columns (Project / Start / etc.) remain
    frozen when scrolling sideways, exactly as before - so the top-left corner stays put
    in both directions.

BUILD: next build -> Compiled successfully.
