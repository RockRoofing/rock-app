pkg274 - Report App Improvement: portal styling reverted, Site App link orange
===============================================================================

NOTE: pkg274 REPLACES pkg272 AND pkg273. It contains everything they had, with the
portal button styling undone. Deploy this one and ignore both earlier packages.


CHANGED FILES
-------------
  middleware.js                           Site App report fix (from pkg273)
  components/ReportProblemButton.js       attachments + screenshot paste (pkg272)
  pages/api/report-problem.js             store attachments, email them (pkg272)
  pages/admin/problem-reports.js          show attachments (pkg272)
  components/OperationsNav.js             bigger sub-tabs ONLY (13.5 -> 15)
  pages/forms/index.js                    Site App link orange + slightly bigger

No new files. All are straight replacements.


IMPORTANT - IF YOU ALREADY DEPLOYED pkg273
-------------------------------------------
pkg273 turned the Report App Improvement link into an orange button in EIGHT portal
places. That is now undone. These seven files are back to EXACTLY their original
contents and are NOT in this package, so re-deploying will not touch them:

  pages/index.js
  pages/design/index.js
  pages/business-financials.js
  pages/project/[id].js
  components/PreContractNav.js
  components/CommercialNav.js
  components/BizNav.js

If you deployed pkg273, you need to restore those seven from pkg272 or earlier - or
simplest, tell me and I will send them as a revert package. They are only a styling
change, nothing functional, so there is no rush either way.

components/OperationsNav.js IS in this package because it also carries the bigger
sub-tabs. Its Report App Improvement button is back to the original gold text; the only
remaining change in that file is the tab size.


WHAT CHANGED IN THE SITE APP
-----------------------------
Still plain underlined writing at the bottom of the operative menu - NOT a button.

  colour   #9a3412 (dark red-brown)  ->  #ea580c (orange)
  size     13px                      ->  15.5px
  weight, underline and position all unchanged.


STILL INCLUDED FROM THE EARLIER PACKAGES
-----------------------------------------
1. Site App reports now actually work. /api/report-problem was missing from the
   middleware passthrough on the siteapp. host, so the POST was rewritten to /forms and
   never reached the API. Reports raised from the Site App before this deploy were never
   saved and cannot be recovered.

2. Portal improvement reports take attachments, including pasted screenshots
   (Win+Shift+S then Ctrl+V). Images embed inline in the office email and show as
   thumbnails on Admin > App Improvements.

3. Operations sub-tabs 13.5px -> 15px (the white strip under the dark header).


TESTED
------
npm run build - Compiled successfully.
Seven reverted portal files confirmed byte-identical to the original repo.
