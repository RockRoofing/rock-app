DEPLOY NOTES - Budgets page crash: error boundary to reveal the real error
==========================================================================

WHAT HAPPENED
-------------
After syncing Xero you got "Application error: a client-side exception has occurred".
That is a React render crash on the Budgets page. The generic message hides the real
cause, and I can't see your browser console from here - so this build wraps the page
in an ERROR BOUNDARY. Instead of a white screen, it will now show the ACTUAL error
message on the page (red box), which tells us exactly what to fix.

I have NOT added the new features you requested (reconciliation checkbox, forecast
lock-in cards, 3-month-average card, bar chart) yet - building them onto a crashing
page would compound the problem. Let's fix the crash first, then I'll add them.

WHAT TO DO
----------
1. Deploy this file (single edit), wait for Vercel "Ready", hard-refresh the page.
2. If it now shows a RED error box with a message, copy that message to me. That one
   line will pinpoint the cause and I'll fix it precisely.
3. If instead the page now loads fine (the boundary can also recover transient
   states), great - tell me and we proceed to the new features.

Also useful if you can: open the browser console (F12 -> Console) after the sync and
paste the first red error line - it often names the exact file/line.

LIKELY SUSPECTS (so you know what I'm looking for)
--------------------------------------------------
The crash started after the sync populated 13 months + the transaction ledger, so
it's probably an unexpected data shape from the benchmark or ledger reaching the
render. The boundary will confirm which.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/budgets.js  (adds an error boundary; no behaviour change)

BUILD
-----
Validated with `next build` -> "Compiled successfully".
