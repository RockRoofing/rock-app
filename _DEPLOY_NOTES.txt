pkg341 - It is in the table. Here is how to find it.
=====================================================

REPLACES pkg302 through pkg340. Deploy this one only.

CHANGED FILES
-------------
  pages/crm.js
  pages/api/crm.js        unchanged since pkg338
  lib/crmImportMap.js     unchanged since pkg305


YOUR NUMBERS SAY IT IS THERE
-----------------------------
    In the shared store and outstanding: 1025    (it was 1024 - the extra one is yours)
    On won/lost projects: 660                    (UNCHANGED - so yours is NOT one of them)

That combination is conclusive. Your activity saved, it is in the shared store, and its
project is open - so it is one of the 365 rows the table is showing.

The trouble is finding it. The table sorts by due date, OLDEST FIRST, across 365 rows. An
activity you set today with a due date next week sits right down at the bottom. It looks
missing when it is just a long way down the list.


WHAT I HAVE ADDED
-----------------
1. A SEARCH BOX on the Activities tab. Type any part of the project name, the activity
   text, the person or the customer and the table narrows to it. Type a word from your
   activity and it will be the only row on screen.

2. THE DIAGNOSTIC PANEL NOW SCROLLS. It was running off the bottom of the window with no
   way to reach the rest, which is why you could not read the trace - the part that would
   have told you all this. Capped at 45% of the window height with its own scrollbar.


THE QUICKEST WAY TO CHECK, RIGHT NOW
-------------------------------------
Rather than scrolling 365 rows: click the "Due date" header once to sort newest first, and
your activity will be at or near the top. Or type its text in the new search box.


WHAT THIS MEANS FOR EVERYTHING BEFORE IT
------------------------------------------
The saving has been working since pkg338. The last few rounds have been about display -
first the open-projects rule, now sort order and the missing scrollbar. I should have
spotted that a to-do list sorted oldest-first buries anything you have just created, and
that a diagnostic panel you cannot scroll is no diagnostic at all.
