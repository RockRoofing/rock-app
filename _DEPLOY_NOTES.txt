Bookkeeping — click a month on the graph to filter the table
============================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy over the top. Frontend only.

CHANGES
-------
1. CLICK-TO-FILTER: click any month on the graph and the table below filters to
   that month, so you can see exactly what makes up that month's value. The graph
   caption shows "Showing <Month> — clear" (click to clear back to all months).
   Works by clicking anywhere in that month's column, or the point.

2. CONFIRMED PER-TAB (your question): the graph already shows ONLY the current
   tab's uncategorised items — costs on the Costs tab, sales on Sales, wages on
   Wages. It does NOT combine all three. (No change needed; confirmed in code.)

FILE CHANGED
------------
• pages/bookkeeping.js — graph onClick sets the month filter; clickable caption
