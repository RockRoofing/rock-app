DEPLOY NOTES - Lessons table: admin-only delete + multi-select
==============================================================

TWO files, both EDITS - just replace them.
  pages/lessons-learnt.js       (admin-gated delete UI + tick boxes / select all)
  pages/api/lessons-learnt.js    (server enforces admin on delete/clear + bulk delete)

WHAT CHANGED (Lessons table tab only)
- DELETE IS NOW ADMIN-ONLY. Only portal users with the "admin" role see the delete
  controls, and the server rejects any delete/clear from non-admins (so it's properly
  enforced, not just hidden).
- MULTI-SELECT DELETE. Admins get a tick box on each row plus a "select all" tick box in
  the header. Selecting rows shows a red bar: "N selected -> Delete selected". You can
  also clear the selection.
- The single-row "x" delete and the "Clear table" button are also admin-only now.

Non-admins can still view, search, filter, add lessons, and edit department tags - they
just can't delete.

BUILD: next build -> Compiled successfully.
