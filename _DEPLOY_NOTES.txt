OPERATIONS SCORECARDS — trend lines/graphs, year date filter, percentage metrics (matches pre-contract)

pages/api/ops-scorecards.js  (REWRITTEN)
  - Now returns a MONTHLY SERIES for the requested range (?from=&to=), so every metric has a trend line.
  - Reuses the portal's /api/forms-missing engine (server-side) to get required-vs-completed per form per
    week, then buckets by month — so the % metrics use exactly the same logic as the portal.
  - Metrics reframed as requested:
      Will & Mike (CM):
        • Gross margin — live & defects projects (latest value; point-in-time).
        • Pre-Start Notifications — % completed vs required (their Pre-Start rows).
        • H&S incidences — count (dedup per project/month).
        • Water Ingress — Rock at fault — count.
        • Procurement savings complete — % of their CLOSED projects with a full savings doc; N/A (—) if
          none closed in range (point-in-time, latest).
        • Issues resolved on time — % of issues resolved in-month that were on/before the required date.
      Dori (Ops):
        • Start On Site / Daily Site Diary / Work Area Handover — % completed vs required.
        • Toolbox Talk — manual Yes/No per month (toggle on the latest month).
        • Tasks completed on time — % of closed tasks that weren't past due.
        • Risk log completed on time — % of resolved risks that met the target resolution date.
  - POST { month, toolbox } unchanged (manual Toolbox Yes/No).

pages/operations/scorecards.js  (REWRITTEN)
  - Trend-line graphs on every time-series card (blue value line + dashed grey trend line), same recharts
    style as the pre-contract scorecard.
  - Year date filter (From / To) defaulting to the last 12 months, like pre-contract.
  - Gross margin & Procurement are point-in-time so they show the latest value (no misleading trend line);
    all other metrics show the monthly trend.

pages/api/targets.js
  - Default targets updated to the new percentage keys (psnPct, procPct, issuesOnTimePct, sosPct, diaryPct,
    wahPct, tasksPct, risksPct).

Deploy all files. No cron/env change.

CONFIRM AFTER DEPLOY:
  - Will/Mike tabs match a CM name CONTAINING "Will"/"Mike". If the names differ, tell me the exact ones.
  - % metrics show "—" when there's nothing required in a month (e.g. no PSNs due, no projects closed);
    that's the intended N/A behaviour.
