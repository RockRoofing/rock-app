DEPLOY NOTES - VAT filter fix + rolling margin fix + full-width pages
=====================================================================

All SIX files are EDITS - just replace them.

1) VAT REFUND - filters now actually work
   - The table and bar chart now refresh when you change Financial year / From / To.
     (Previously the page only loaded once and ignored filter changes.)
   - Also filters the rows client-side to the exact month window, so what you see
     always matches the dates chosen.

2) MARGIN - 12-month rolling now plots backwards
   - Previously only the latest month showed (it required a FULL 12 months before
     plotting a point). Now every month plots, using up to 12 trailing months
     (fewer for the earliest months), so the rolling line runs across the range.

3) FULL WIDTH pages
   Margin, Retentions Due, VAT Refund, Cash Schedule, Cash Flow and Summary now
   span the full window width (were previously capped ~1200-1320px and centred).

FILES
  pages/business-financials.js                        (Summary - full width)
  pages/business-financials/margin.js                 (rolling fix + full width)
  pages/business-financials/vat-refund.js             (filter fix + full width)
  pages/business-financials/retentions-due.js         (full width)
  pages/business-financials/cash-schedule.js          (full width)
  pages/business-financials/cashflow.js               (full width)

STILL TO DO (waiting on your screenshots)
  You mentioned some Margin / Sales figures don't look right. Send the images when
  you can. Most likely cause: the monthly P&L totals (income / cost of sales /
  overheads) come from the Account Categorisation config via the benchmark sync -
  if any account code is uncategorised or in the wrong section, the margins and
  sales will be off. I'll confirm once I can see the numbers.

BUILD: next build -> Compiled successfully.
