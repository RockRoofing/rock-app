SITE APP — CM access now gated by access level + Operative badge styling

  pages/forms/index.js
   - FIX: the Contracts Manager sections were showing to anyone whose NAME matched a project's Contracts
     Manager field, even if their Site App access level was set to Operative. CM access is now gated purely
     by the access level (accessLevel === 'contracts-manager'). Name-matching still decides which projects
     are "theirs", but no longer grants CM access. Setting a user to Operative now hides all CM sections.

  pages/operations/users.js  (Site App Users page)
   - The "Operative" access value now renders as a pill badge (grey/slate) matching the style of the
     "Contracts Manager" pill, instead of plain text.

Deploy both files. No cron/env change.
