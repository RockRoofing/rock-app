DEPLOY NOTES - FIX: Cash Flow page client-side exception
========================================================

ONE file, an EDIT - replace the pages/project-cashflow.js you added.
  pages/project-cashflow.js

WHAT WAS WRONG
  The page crashed with "a client-side exception has occurred". Cause: a React hook
  (a useMemo) was declared AFTER the early "if (loading) return" - so once the planner
  data loaded, React ran more hooks than on the first render (Rules of Hooks
  violation) and threw. Also added defensive guards on data.allocations / data.meta.

FIX
  Moved all hooks above the loading return; guarded the data accesses. No behaviour
  change - same mirrored greyed gantt + period selection.

Deploy this over the version you added and the page will load.

BUILD: next build -> Compiled successfully.
