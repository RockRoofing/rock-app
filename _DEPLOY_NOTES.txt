DEPLOY NOTES - RFI open window refinements
==========================================

ONE file, an EDIT - just replace it.
  pages/design/[project]/rfis.js

CHANGES (all inside the open-RFI window)
  1. COMMENTS moved ABOVE the mark-up viewer, and put in a clear light-blue panel with
     each comment on its own white card - much easier to read.
  2. MARK RESOLVED / RE-OPEN stays Rock Roofing (internal) only - customers can't resolve.
  3. Removed the DUPLICATE Download button. There's now just the one (inside the mark-up
     viewer's toolbar).
  4. EDIT is now INLINE - clicking Edit reveals the editable fields (Required by, Customer
     responsible, Description, Attachments) right inside the open RFI, with Save / Cancel.
     It no longer opens a separate pop-up. (Issued date stays automatic.)
     "+ Add RFI" (new RFIs) still uses the normal add form.
  5. CUSTOMERS can only comment and mark up the drawings - Edit, Mark resolved, Delete and
     Add RFI remain internal-only (unchanged, confirmed).

BUILD: next build -> Compiled successfully.
