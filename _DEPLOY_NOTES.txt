Bookkeeping — default to uncategorised items on all tabs
========================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy over the top. Frontend only.

CHANGE
------
Every tab now opens with the "No category assigned" filter applied, so you land
straight on the items needing attention (combined with the most-recent-full-month
default from the previous update). Applies on load and when switching tabs.

To see everything, change the filter to "Categorised & uncategorised" or click
"Clear filters" (Clear resets to all categories + all months).

FILE CHANGED
------------
• pages/bookkeeping.js — assigned filter defaults to 'no' (uncategorised)
