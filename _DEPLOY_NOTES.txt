PRE-START NOTIFICATION PDF — proper branded document (was rendering as a Site Issue Report)

WHAT WAS WRONG (per screenshot)
  The PSN sent to the customer was built with the Issue-report PDF builder, so it showed "Site Issue
  Report", an "Issue ID", a "STATUS: OPEN" box, and the answers as raw field codes (f_7, f_2, f_13...).

FIX
  lib/preStartNotifyPdf.js  (NEW)
   - A dedicated Pre-Start Notification PDF builder. Rock-branded header titled "Pre-Start Notification"
     (no issue/status language). Renders the REAL question labels grouped by the form's sections, with
     answers formatted properly (dates as DD/MM/YYYY, single/multi choices as their labels, photos as a
     count, signature as the name). The removed "email of requester" field (f_4) is skipped.

  pages/api/pre-start-notify-send.js  (REWRITTEN)
   - Now loads the PSN form definition (for the labels) and the project, and builds the PDF with
     buildPsnPDF instead of buildIssuePDF. Everything else (recipients, Resend send, statuses) unchanged.

Deploy both files. No cron/env change. Re-send a PSN to see the corrected document.
