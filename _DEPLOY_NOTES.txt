OPS PROJECT FINANCIALS — full-bleed (no longer looks "inserted")
  pages/operations/project-financials.js
   - Removed the padded card container + rounded corners around the embed.
   - The embed now sits edge-to-edge directly under the Ops nav (single 52px bar),
     filling the window, so it reads as one continuous native page.
