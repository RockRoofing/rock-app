DEPLOY NOTES - Add Cash Flow tab to the remaining Commercial pages
==================================================================

SIX files, all EDITS - just replace them.
  pages/commercial.js            (this is the "Project Financials" tab page)
  pages/outstanding-invoices.js
  pages/retention.js
  pages/variations.js
  pages/application-calendar.js
  pages/commercial-scorecard.js

WHAT WAS WRONG
  Only Contracted Rates, Applications and WIP used the shared nav component, so adding
  the Cash Flow tab there didn't affect the other pages - each of those hardcodes its
  own tab row. The Cash Flow link was missing on them.

FIX
  Added the "Cash Flow" tab (-> /project-cashflow) to each of these pages' nav rows,
  between WIP and Commercial Scorecard, matching the others.

NOTE: pages/project-financials.js is a different (per-project) page with no commercial
tab bar, so it needs no change.

BUILD: next build -> Compiled successfully.
