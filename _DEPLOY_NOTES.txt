ADMIN NAV — Documents + App Improvements tabs now show on every admin page

CAUSE: the Portal Users page (pages/admin.js) and the Templates page (pages/admin/templates.js) each have
their OWN hardcoded copy of the admin tab list — they don't use the shared AdminShell — so they still
showed only the original 4 tabs (Portal Users, Templates, Form Builder, Site App Users). The newer pages
(Documents, App Improvements) use AdminShell and already had all 6.

FIX: added 'Documents' and 'App Improvements' to the inline tab lists on both pages, so all admin pages now
show the same 6 tabs.

  pages/admin.js
  pages/admin/templates.js

Deploy both files. No cron/env change.
