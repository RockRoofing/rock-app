PROCUREMENT SAVINGS v5 — fixes
  components/ProcurementSavings.js  (Qty/Unit inputs given firm min-widths)
  components/ContactPicker.js       (dropdown now fixed-position so it isn't clipped by the table scroll)

NOTE: The earlier screenshot showed NO "Package Name" column, which means the v3/v4
component wasn't deployed yet. Make sure THIS ProcurementSavings.js (with Package Name
as the first column) actually deploys. If Package Name still doesn't show after deploy,
the file didn't update on the live site.
