DEPLOY NOTES - Issues: photos no longer mandatory
==================================================

TWO files, both EDITS - just replace them.
  pages/forms/issue.js                              (Site App - raise an issue)
  pages/operations/project-management/issues.js     (Project Management - create issue)

CHANGE
  Creating an issue no longer requires a photo, in BOTH places:
   - Site App "Raise an issue": the "Attach at least one photo" validation is removed
     and the Photos label now reads "Photos (optional)".
   - Project Management "New issue": the "Attach at least one photo" validation is
     removed.
  You can still add photos - they're just no longer required to submit.

BUILD: next build -> Compiled successfully.
