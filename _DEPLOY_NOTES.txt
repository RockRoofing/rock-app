DEPLOY NOTES - Isolate the invalid_scope: remove taxreports for now
===================================================================

Reconnect still failed with invalid_scope even after removing finance.statements.read.
That means accounting.reports.taxreports.read is ALSO being rejected by your app - even
though it appeared in the authorisation list, it isn't grantable in the OAuth flow (Xero
sometimes lists a scope that the app tier can't actually request).

So we can't pull the filed VAT return via that scope. This build removes it and returns
connect.js to a WORKING state (keeps accounting.banktransactions.read, which the Sales
page needs).

DEPLOY + TEST
-------------
1. Replace pages/connect.js. Commit -> Vercel "Ready".
2. Go to /connect and reconnect Xero. The consent screen SHOULD now load (no
   invalid_scope). Approve it.
3. If it STILL says invalid_scope, then the culprit is accounting.banktransactions.read -
   tell me and I'll remove that too and we'll know exactly which scope your app rejects.

WHAT THIS MEANS FOR VAT
-----------------------
If taxreports genuinely can't be granted, we can't read the filed return via the standard
accounting API. Options I'll lay out once you confirm the reconnect works:
  (a) Check the Xero Developer app config - the scope may need enabling there (like the
      journals one did), OR the app may need to be a "custom connection"/certified app to
      get taxreports.
  (b) Fall back to: manual entry of the filed Box 5 per past month (quick, reliable), with
      the transaction estimate for current/future. Least moving parts.
  (c) You export the VAT return (like the xlsx you sent) and we import it.

FILE
----
  - pages/connect.js

BUILD: next build -> Compiled successfully.
