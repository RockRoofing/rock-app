PLANNING — darker Actual, Send WLA button, stacked multi-week send pop-out

  pages/operations/project-management/planning.js
   1) ACTUAL cells are now a darker/medium green (#4ade80) — clearly darker than Confirmed, lighter than
      the dark-green status line.
   2) Removed the small download button / click-to-open from the W/C column headers (now plain labels).
   3) New "Send Weekly Labour Allocation" button top-right (in the controls row).
   4) Clicking it opens a pop-out that IS the send flow:
        - Filters at the top: how far ahead (1/2/3/4 weeks) + per-operative de-select (untick people).
        - Below, one table PER WEEK, stacked: "This week" on top, "Week +1" below, "Week +2" below that,
          etc. Each shows the per-operative grid + daily totals + colour key.
        - Buttons: Download PDF (this week) and "Send to operatives" (emails future-only allocations
          across the selected number of weeks to the ticked operatives).

Deploy over previous planning.js. No API change (uses existing planning-week / -email / -pdf).
