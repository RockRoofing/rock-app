DEPLOY NOTES - Invoices Owed: date-range removed, due/expected basis toggle
===========================================================================

CHANGES
-------
1) REMOVED the "Due between ... and ..." date-range filter (and its Reset).
2) ADDED a "Date basis" toggle: Expected | Due. It controls which date the page uses
   for the charts (month buckets), the sort, and the Overdue column.
   - DEFAULT = Expected.
   - Expected falls back to the invoice's due date when no expected date has been set,
     so nothing disappears.
3) The two charts and the Overdue column now follow the chosen basis; titles/subs note
   "(by expected date)" / "(by due date)".

Everything else (ticks + selected total, customer multi-select filter, sortable
columns, comments, full width) is unchanged. The whole outstanding list now shows (no
date window) - use the customer filter / column sort to navigate.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/invoices.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh.

VERIFY
------
  - No date-range inputs; a Date basis toggle (Expected | Due) defaulting to Expected.
  - Switching to Due re-buckets the charts and re-computes Overdue against the due date.
  - Setting an Expected date on a row moves it in the Expected-basis charts/sort.

BUILD
-----
Validated with `next build` -> "Compiled successfully". Hooks confirmed above the early
return.
