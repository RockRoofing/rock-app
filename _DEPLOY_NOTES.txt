Application PDF — Summary on its own page, certificate heading removed
=====================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single-file change (lib/applicationPdf.js).

CHANGES
-------
1) SUMMARY ON ITS OWN PAGE
   Contract Works now starts on a fresh page, so page 1 is just the Summary +
   certificate figures.
2) "PAYMENT CERTIFICATE" HEADING REMOVED
   The gross/MCD/sub-total/retention/total block now sits directly under the
   Summary with no separate title.

FILES
-----
• lib/applicationPdf.js
