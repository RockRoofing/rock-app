SRATs v2
  pages/operations/project-management/srat.js
   - Even column widths (fixed table layout + colgroup).
   - New Date column; table auto-sorts by Date (newest first). Date field added to the
     add/edit modal (defaults to today) and shown in the large View.
   - Situation / Roadblocks / Actions / Timeline cells are clickable -> open the full text
     in a larger pop-up window (view-only).
   - Pagination: max 50 SRATs per page.
