PLANNING — operative email: no attachment, new heading, project addresses list

  pages/api/planning-week-email.js
   - Removed the PDF attachment — the email is now text/HTML only.
   - Heading and subject changed to "Your upcoming project allocation".
   - Below the day-by-day allocation, a "Project addresses" section lists each unique project the
     operative is assigned to, with its address (shows "Address not set" if the project has no address).

  pages/api/planning-week.js
   - The week assembler now carries each project's ADDRESS through (from the IHM projectAddress /
     siteLocation for live projects, or the deal's site/organisation for negotiated), so the email can
     list addresses. (projectNameMap now returns { name, address }.)

Deploy over previous. No env / cron change. Uses existing Resend setup.

NOTE: addresses come from each project's record. If some show "Address not set", set the project address
on the Project Details tab (or the IHM) and it'll appear.
