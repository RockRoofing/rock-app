DEPLOY NOTES - Overhead Budgets: full width, hide-persist fix, frozen totals row
================================================================================

CHANGES
-------
1) HIDE-COLUMNS NOT SAVING - FIXED. The show/hide selection now saves IMMEDIATELY
   whenever you tick/untick an account (or use Show all / Hide all), sending the
   fresh value to the server rather than relying on a later click. The previous
   version could save a stale value, so hidden rows reappeared on refresh. Now the
   selection persists for you and everyone on reload. ("Done" just closes the menu.)

2) FULL WIDTH. The page/table now span the full screen width (was capped at 1700px).

3) TOTALS ROW AT THE TOP, FROZEN. A bold TOTAL row sits directly under the header and
   sums every column across the visible accounts (monthly actuals/forecasts, Budget/mo,
   and all four summary columns). Both the HEADER row and the TOTAL row are frozen
   (sticky) so they stay visible while you scroll the accounts. Hidden accounts are
   excluded from the totals.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/budgets.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh.

VERIFY
------
  - Table fills the screen width.
  - Hide an account via the gear, refresh the page -> it stays hidden.
  - Scroll the accounts -> the header and the TOTAL row stay pinned at the top.
  - Totals reflect only the currently-visible accounts.

NOTE
----
The frozen totals row sits at top:34px (just under the sticky header). If your header
wraps to two lines on a very narrow screen the offset may look slightly off - tell me
and I can measure the header height dynamically instead of using a fixed 34px.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
