PLANNING — status must be chosen + Clear selection fix

  pages/operations/project-management/planning.js

  ALLOCATION STATUS NOT PRE-SELECTED
   - When adding labour, no status is selected by default (previously defaulted to Confirmed).
   - You can't allocate until you pick Confirmed / Provisional / Actual: the Allocate button is disabled
     and greyed until a status is chosen, the label shows "— choose one to allocate" in red, and saving
     without one shows an error. (Edit mode still pre-loads the existing status if the selected days share
     one.)

  CLEAR SELECTION FIX
   - "Clear selection" now also resets the drag state and stops event propagation, so the day highlights
     and the amber selection bar reliably clear on click (previously a lingering drag state could
     immediately re-select cells as the cursor moved).

Deploy over previous planning.js. No API change.
