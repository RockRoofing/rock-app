DEPLOY NOTES - FIX: Cash Flow page client-side exception
========================================================

ONE file, an EDIT - just replace it.
  pages/business-financials/cashflow.js

WHAT WAS WRONG
  The Cash Flow page threw "a client-side exception has occurred" on load. Cause: a
  leftover CIS-diagnostic message block was still referencing three variables
  (autoCount / anyLineCodes / codeSet) that had been removed. The page compiled fine
  but crashed at render because those variables no longer existed.

FIX
  Removed the orphaned diagnostic block. The bills table and CIS auto-tick behaviour
  are unchanged and intact.

Deploy this and the Cash Flow page will load normally again.

BUILD: next build -> Compiled successfully.
