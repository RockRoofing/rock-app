DEPLOY NOTES - Handover Docs mirrored into Pre-Contract
=======================================================

SIX files. TWO are NEW (add via GitHub "Add file"), FOUR are EDITS.

  NEW   components/HandoverDocs.js
  NEW   pages/pre-contract-handover-docs.js
  EDIT  components/PreContractNav.js               (adds the Handover Docs tab)
  EDIT  pages/api/design-handover-docs.js          (allows Pre-Contract users)
  EDIT  pages/api/design-handover-zip.js           (allows Pre-Contract users)
  EDIT  pages/design/[project]/handover-docs.js    (now uses the shared component)

*** Create the two NEW files via "Add file" at those exact paths. ***

WHAT THIS IS
  Handover Docs now appears in BOTH the Design portal AND the Pre-Contract section, and they
  are a TRUE MIRROR - both read and write the SAME files (same underlying store). Upload or
  delete in one and it shows in the other. Both allow full upload / delete.

  - Design portal: unchanged for the user - same page, same behaviour (it now shares one
    common component with the Pre-Contract version so they can never drift apart).
  - Pre-Contract: a new "Handover Docs" tab. It opens a project picker (search + click a
    project), then shows the exact same Handover Docs page for that project.

  Still internal-only - customers never see Handover Docs in either place.

BUILD: next build -> Compiled successfully.
