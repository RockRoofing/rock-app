Bookkeeping — Overheads default + searchable supplier filter
============================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully

CHANGES
-------
1. OVERHEADS TAB DEFAULT
   Switching to the Overheads tab now defaults to:
     • Categorised only  (was "No category assigned")
     • All months        (was the tab's default month)
   Other tabs unchanged (still default to uncategorised + their default month).
   [pages/bookkeeping.js -> switchTab(): special-cases 'ignored']

2. SEARCHABLE SUPPLIER / CUSTOMER FILTER
   The supplier (and customer, on Sales tab) dropdown is now a type-to-search
   combobox: click it, start typing, and the list filters live by what you type.
   Click a match to select; the top row clears back to "All suppliers/customers".
   [pages/bookkeeping.js -> new SupplierPicker component replaces the <select>]

FILE CHANGED
------------
• pages/bookkeeping.js
