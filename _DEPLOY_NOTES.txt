pkg340 - Your activity IS saved. The open-projects rule is hiding it.
======================================================================

REPLACES pkg302 through pkg339. Deploy this one only.

CHANGED FILES
-------------
  pages/crm.js
  pages/api/crm.js        unchanged since pkg338
  lib/crmImportMap.js     unchanged since pkg305


WHAT YOUR NUMBERS PROVED
-------------------------
    In the shared store and outstanding: 1024

It was 1023 before you added yours. That one extra IS your activity. It saved, it went to
the shared store, and it is sitting alongside the Pipedrive ones exactly as you wanted.

    On won/lost projects: 660

And that is what is hiding it. The project you added it to is not open, and pkg336
restricted this tab to open projects - at your request, and it is doing exactly that.


TWO THINGS I HAD WRONG ON THAT PANEL
-------------------------------------
1. "Added in the CRM: 0" was misleading. It counted activities held ON a deal, and since
   pkg338 nothing is held there - everything goes to the shared store, where yours and the
   imported ones are indistinguishable. That is the single-store behaviour you asked for,
   but the label made it look like nothing had saved. It now reads:

       Still held on a deal (pre-update): 0
       In the shared store and outstanding: 1024 - imported and yours together

2. The per-activity trace only looked at activities held on a deal, so with nothing there
   it printed nothing - just when it was most needed. It now traces the shared store too
   and names the rule excluding each one:

       Round Silo - Call back Tuesday (2026-08-25)
           hidden: project is "won" - this tab lists open projects only


THE DECISION IS YOURS
----------------------
The tab lists open projects only because you asked for that in pkg336, and I have left it
alone. But 660 outstanding activities are sitting behind that rule, and your test is one of
them.

Say which you want and it is a one-line change:

  a) open projects only            - as now
  b) open and won                  - excludes lost jobs only; my recommendation, since you
                                     still chase variations and follow-ups after a win
  c) everything

Given how long this has taken I did not want to change it again without you saying so.
