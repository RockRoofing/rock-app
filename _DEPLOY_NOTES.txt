PLANNING — View WLA: W/C dropdowns + PDF only (table removed)

  pages/operations/project-management/planning.js  (ViewWeekModal)
   - From / To are now W/C DROPDOWNS listing "W/C <date>" (2 years back to 1 year forward), not free
     date pickers. Picking a From past To auto-bumps To; To only offers weeks >= From.
   - Removed the on-screen table and the "No labour allocated for the first week…" message (the range
     can be long; it's just for downloading).
   - Shows the list of W/C weeks that will be in the PDF, then Download PDF (one page per week, max 26).

Deploy over previous planning.js. No API change.
