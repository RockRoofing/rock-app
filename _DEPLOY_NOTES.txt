pkg277 - Operations navigation at 17px
=======================================

NOTE: pkg277 REPLACES pkg272 through pkg276. Same file list, nav size changed from 20px
to 17px. Deploy this one and ignore the earlier five.


CHANGED FILES
-------------
  components/OperationsNav.js             top navigation 20px -> 17px
  pages/forms/index.js                    Site App report: attachments + screenshot paste
  middleware.js                           Site App report fix
  components/ReportProblemButton.js       portal attachments + screenshot paste
  pages/api/report-problem.js             store attachments, email them
  pages/admin/problem-reports.js          show attachments

No new files. All are straight replacements.


NAVIGATION SIZING
-----------------
The dark top row of the Operations portal:

  link + active text   17px   (was 13px originally, briefly 20px in pkg275)
  dividers             16px
  bar height           58px
  logo                 32px

Dividers, bar height and logo were scaled back down from the 20px version so the row
stays in proportion. 17px should fit a normal laptop screen without the sideways
scrolling that 20px risked.

The white sub-tab strip underneath is unchanged at 15px.


TESTED
------
npm run build - Compiled successfully.


STILL OUTSTANDING
-----------------
The zip I am working from is pkg264 and does not contain pkg265. Please confirm whether
pkg265 was deployed - if it was, send a fresh export before the next package.
