PROCUREMENT SAVINGS v3 — grid changes + contacts

DEPLOY (mirror into repo, overwrite):
  pages/api/contacts.js               (NEW — global contacts directory)
  pages/api/procurement-savings.js    (unchanged from v2, included for safety)
  components/ContactPicker.js         (NEW — searchable add/select contact)
  components/ProcurementSavings.js    (rewritten grid — all the changes)
  pages/procurement-savings.js        (needs-finalising now: not-started only, warning)

NOTE: this builds on procurement-savings-v2. If you haven't deployed v2 yet,
also deploy from that batch: components/PreContractNav.js, components/OperationsNav.js,
pages/operations/projects.js  (they add the tabs/sub-tab and remove the old Ops page).
And delete pages/operations/project-management/procurement-savings.js if it exists.
