ROLES — fixes

  pages/admin.js
   BUG FIX: selecting a role (e.g. Pre-Contract) didn't "take". Cause: the role dropdown
   defaulted to value 'standard', which is no longer an option in the list, so the select
   showed a mismatched value and the change didn't register. Now the dropdown always shows a
   real option (legacy 'standard' normalised to Post-Contract on display and on edit-load).
   Also updated the helper text to list the four roles.

  pages/api/ops-users.js
   Site App user management (add / reset-PIN / delete / list) widened from Admin-only to
   MANAGEMENT + ADMIN, per request. Operative login/set-pin remain open (Site App unaffected).
   Templates remain ADMIN-ONLY (unchanged).

Deploy over the versions from the roles-restructure-enforcement package.
