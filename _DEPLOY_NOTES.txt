DEPLOY NOTES - FIX: Cash Flow client-side exception
===================================================

ONE file, an EDIT - replace it.
  pages/project-cashflow.js

WHAT WAS WRONG
  The daily-totals change added a useMemo (cashByDay) AFTER the "if (loading) return",
  so once the data loaded React ran an extra hook (rules-of-hooks violation) and the
  page threw a client-side exception.

FIX
  Moved cashByDay above the loading return with the other hooks. No behaviour change -
  daily in/out totals and retention releases work as intended.

Deploy this over the previous "Cashflow daily totals" version.

BUILD: next build -> Compiled successfully.
