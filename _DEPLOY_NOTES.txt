DEPLOY NOTES - Cash Flow forecasts: colours, values on gantt, multi-materials
=============================================================================

TWO files, both EDITS - just replace them.
  pages/api/project-cashflow.js    (adds "all forecasts" fetch for gantt colouring)
  pages/project-cashflow.js

CHANGES
1. Renamed "Hypothetical" -> "Forecasted" throughout.

2. MULTI-DAY SELECTION now fills the whole span between the first and last cell you
   drag across (so you can grab an entire application period in one drag).

3. MATERIALS are now MULTIPLE LINE ITEMS. Each line has:
     - Add by: % of budget OR £ figure
     - Value
     - Comment (e.g. supplier name)
     - its own Delivery day (cash out)
   Add as many as you need (one per supplier / delivery). Total shown at the foot.

4. FORECASTS SHOWN ON THE GANTT as coloured bands across their period:
     - App 1 red, App 2 blue, App 3 green, App 4 amber, App 5 purple, then repeat.
     - A colour KEY is shown at the top.
     - Each band shows Revenue, Labour and Materials for that period.
     - The materials DELIVERY DAY is drawn as a DARKER cell within the band.
   (Week view tints the weeks a forecast covers in its colour.)

Forecasts remain forecast-only (stored separately, never written to real
applications).

BUILD: next build -> Compiled successfully.
