DEPLOY NOTES - Parallel CRM Sales Dashboard (for comparison)
============================================================

Purpose: run a SECOND Sales Dashboard sourced from the CRM ALONGSIDE the existing one, so you
can compare them before switching over. The old dashboard is UNTOUCHED and stays on the live
Pipedrive API. *** Deploy THIS package instead of the earlier "Retie dashboards" package. ***

FOUR files. THREE NEW (add via "Add file"), ONE EDIT.

  NEW   lib/crmDashboardAdapter.js  (translates CRM deals -> the flat dashboard shape)
  NEW   pages/api/deals-crm.js      (parallel deals endpoint, reads crm:deals)
  NEW   pages/sales-crm.js          (the CRM-sourced Sales Dashboard, at /sales-crm)
  EDIT  components/PreContractNav.js (adds a temporary "Sales (CRM)" tab)

*** Create the THREE NEW files via "Add file" at those exact paths. ***

IMPORTANT - the nav EDIT:
  This PreContractNav.js already includes the "CRM" tab from the CRM persistence package.
  If you have already deployed that package, just replace PreContractNav.js with this one.
  It adds ONE new tab: "Sales (CRM)", between "Sales Dashboard" and "CRM".

WHAT YOU GET
  - "Sales Dashboard" (existing tab)  -> unchanged, live Pipedrive data.
  - "Sales (CRM)" (new tab)           -> same dashboard, but data from the CRM (imported deals).
    It carries a green banner marking it as the comparison view.
  Open both, compare the numbers. The CRM one only populates once you've imported deals in the
  CRM (Pre-Contract -> CRM -> Import -> Deals export).

WHAT IS DELIBERATELY BLANK on the CRM version (captured going forward, per your steer):
  Customer Type, First-Contact date, Received date - so any metric using those (new-vs-existing
  customer split, first-contact / received timing) will differ from the old dashboard. That is
  EXPECTED, not a mapping error. Everything else (value, stage, status, estimator, sales person,
  region, systems, lost reason, won/created dates) should match once deals are imported.

PREREQUISITE: the CRM persistence + import package must be deployed first (this reads crm:deals).

NO change to /api/deals, /api/negotiating, /sales, /scorecard. No vercel.json change. No cron.
BUILD: Compiled successfully.
