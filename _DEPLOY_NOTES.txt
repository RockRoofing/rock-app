pkg471 - VAT Type, and an audit of every other column
=====================================================

Deploy after pkg470. One changed file: pages/retention.js. No new files.


VAT TYPE - the same mistake again
-----------------------------------
The column is read as vatRateLabel. I wrote it as vatType. Third time in this import:
Gross AFA, Total Paid, and now this - each written under a name nothing reads, so the
value imported fine and the cell showed a dash.

Rather than fix it and wait to be told about the next one, I have now compared EVERY cell
in the row against every field the import writes. That should have been the first thing I
did.


THE AUDIT - what the row reads and the import did not write
-------------------------------------------------------------
    vatRateLabel            FIXED - this is VAT Type
    retentionOwed           FIXED - the two release values added together
    retention612Allocated   FIXED - your "612 Allocated" column

    afaSource               correctly absent - says where the AFA came from, and for a
                            manual row the answer is "you typed it"
    mcdPct, mcdValue,       correctly absent - MCD lives on the project in Commercial.
    mcdRecorded             These rows have no project, so they show 0.00% (default).
    pcDateTBC,              correctly absent - flags from project details, which a
    defectsDateTBC          tracker-only row does not have

So three genuine misses, all fixed, and six that are absent for good reason.


IF GROSS AFA AND TOTAL PAID ARE STILL BLANK
---------------------------------------------
Those two were fixed in pkg469. If they are still empty, either pkg469 is not deployed or
the file has not been imported again since it was.

The import only writes what is in the file at the time it runs - correcting the code does
not go back and repair rows already stored. Re-import after deploying and the nine rows
update in place on Ref.

Worth doing in one go now, so all four columns land together.
