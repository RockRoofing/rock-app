DEPLOY NOTES - O&M page: confirm download who/when banner is live (+ version marker)
====================================================================================

ONE file, EDIT. Replace in GitHub.
(Path uses literal square brackets: pages/design/[project]/oms.js)

  EDIT  pages/design/[project]/oms.js

BACKGROUND
Yes - you did ask for this, and it was built earlier: the "CUSTOMER DOWNLOADED" banner shows the
customer's name (from their portal user account), the date/time they downloaded, and the revision.
The code for it is present and correct. If it isn't showing on your live banner, the most likely
reason is that the earlier oms.js wasn't deployed (or is cached). This package re-delivers it and
adds a small "v2" marker next to the "O&M Manual" heading so you can confirm the new build is live.

HOW TO CHECK
1) Deploy, then open the O&M page. If you see "O&M Manual v2" in the heading, the new code IS live.
   - If you DON'T see "v2", the deploy/cache hasn't updated - hard refresh, or check the file saved
     to the correct path in GitHub.
2) With the new build live, have a CUSTOMER portal user open the O&M page and click the Download
   button (the one on the O&M page - NOT the emailed link). The banner should then read
   "CUSTOMER DOWNLOADED" followed by: their name (company) - date/time - Rev.

IMPORTANT (how the tracking works)
- Only downloads via the Download BUTTON on the O&M page are recorded. Downloads from the emailed
  link are not tracked.
- The green "CUSTOMER DOWNLOADED" banner appears once a customer has downloaded the CURRENT
  revision. If a newer revision was built after their download, it shows red "NOT DOWNLOADED" with
  a note that they downloaded an earlier revision.
- Internal staff downloads do not turn the banner green (it's specifically the customer we track).

Once you've confirmed it's live and working, tell me and I'll send a tiny follow-up removing the
"v2" marker.

NO vercel.json change. No cron. BUILD: Compiled successfully.
