PRE-CONTRACT ACCESS FIXES (Scorecard + Negotiating)

Pre-Contract users were blocked from their own sales tools because these APIs were scoped to
exclude them. Widened to include pre-contract:

  pages/api/negotiating.js   now: pre-contract, post-contract, management, admin
                              (fixes "no access" — Negotiating shows in BOTH Pre-Contract & Operations)
  pages/api/deals.js         now: pre-contract, post-contract, management, admin  (Scorecard/Sales)
  pages/api/targets.js       now: pre-contract, post-contract, management, admin  (Scorecard targets)
  pages/api/sync.js          now: pre-contract, post-contract, management, admin  (Scorecard sync button)

Verified the full Pre-Contract page surface (sales, scorecard, negotiating, procurement-savings):
all APIs they call now allow pre-contract. commercial-metrics and stages remain restricted
(post-contract+) — correct, as Pre-Contract pages don't use them (those are project financials/margins).

Deploy over the previous versions of these 4 files.
