PLANNING — View Weekly Labour Allocations: date-range PDF

  pages/operations/project-management/planning.js  (ViewWeekModal)
   - Replaced the single "Week commencing" picker with a FROM / TO week range.
   - Shows how many weeks the range spans; the on-screen table previews the FIRST week of the range.
   - "Download PDF" now downloads EVERY week in the range (one page per week), labelled with the count.

  pages/api/planning-week-pdf.js
   - Raised the max weeks per PDF from 8 to 26 so date ranges up to ~6 months download in one go.

Deploy over previous. No env / cron change.
