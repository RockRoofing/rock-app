Bookkeeping — fix empty Direct Wages tab (matched API+page) + wages date parsing
================================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy over the top — deploy ALL THREE files together (matched set).

DIAGNOSIS (from your _diag output)
----------------------------------
_diag showed: wagesTabCount=45, untaggedWagesRaw=45, categorisedWageLines=0.
=> The 45 untagged wages ARE in the API response, but the tab rendered empty.
   That's a VERSION MISMATCH: the deployed API returned 45 while an older
   bookkeeping.js page bundle was live and didn't render them. Deploying the
   matched pair together fixes it.
=> categorisedWageLines=0 is a DATA matter, not a bug: no project-tagged wages
   have been uploaded (only the long untagged 2023–2026 file). Upload the recent
   TAGGED wages export to populate categorised wages.

WHAT'S IN THIS PACKAGE
----------------------
1. pages/bookkeeping.js + pages/api/bookkeeping.js — the matched, consistent pair
   (page reads data.wages; API returns wages incl. the 45 untagged). This alone
   makes the Direct Wages tab show your 45 items.
   • Clearer empty-state text: "Nothing here yet — upload the relevant export" vs
     "No items match these filters — try Clear filters".
   • (_diag left in the API response; harmless. Can remove later.)

2. pages/api/import-wages-bulk.js — BUG FIX: excelDate now parses Xero's text
   dates like "28 Feb 2023" / "28 Sept 2023". Previously these stored as raw text,
   so those wage lines had no month and wouldn't group/filter by month. Runtime-
   tested. NOTE: re-upload the wages file after deploying so dates are re-parsed.

ACTIONS AFTER DEPLOY
--------------------
• Direct Wages tab should immediately show the 45 untagged items.
• If you also want the monthly filter/graph to work for old wages: re-upload the
  long wages file (dates get re-parsed to ISO).
• To get CATEGORISED (project) wages: upload the recent project-tagged wages
  export (the tracking-transfer journal style).

FILES CHANGED
-------------
• pages/bookkeeping.js          — matched page (empty-state copy)
• pages/api/bookkeeping.js      — matched API
• pages/api/import-wages-bulk.js — excelDate text-month parsing fix
