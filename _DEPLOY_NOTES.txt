DEPLOY NOTES - Margin table sort (+ January explanation)
========================================================

ONE file, an EDIT - just replace it.
  pages/business-financials/margin.js

CHANGE
  The calculation table now sorts by month with the MOST RECENT at the top
  (Jul/Jun down to Dec). The charts stay left-to-right chronological as before.

ABOUT THE JANUARY FIGURE
  Good spot. Every month in the table ties exactly to your P&L EXCEPT January:
    - P&L (current)      Jan Total Turnover = 170,690.90
    - Margin table       Jan income        = 188,407
    - Difference         ~17,716 (the stored figure is HIGHER)

  This is NOT a calculation bug - the page just displays the income figure stored
  in the benchmark. The other months match, so the maths is right. January is
  stale: it was synced BEFORE ~17,716 of credits/adjustments were posted that later
  reduced January's sales to 170,690.90.

  FIX (no code change): re-run the P&L benchmark sync so January picks up the
  corrected figure. Once re-synced, the Jan income should drop to ~170,690 and the
  margin will recompute automatically.

  If it still doesn't match after a re-sync, it means an account code is mis-
  categorised for January only - send me a shout and I'll trace that specific code.

BUILD: next build -> Compiled successfully.
