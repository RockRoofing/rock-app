DEPLOY NOTES - App Improvements: priority required + priority filter
====================================================================

THREE files, all EDITS - just replace them.
  components/ReportProblemButton.js   (Portal report modal)
  pages/forms/index.js                (Site App report modal)
  pages/admin/problem-reports.js      (admin log - new priority filter)

CHANGES
1. Priority now starts with NOTHING selected (no default). The user MUST pick
   Low / Medium / High before the report can be submitted - if they don't, they get
   "Please select a priority." Applies to both the Portal and Site App report modals.

2. The App Improvements admin page now has a PRIORITY filter (All / High / Medium /
   Low) alongside the existing User and Status filters. Reset clears it too.

BUILD: next build -> Compiled successfully.
