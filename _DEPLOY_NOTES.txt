Bookkeeping — graph polish (recharts) + per-tab + target line + robust months
=============================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully  (recharts already a dependency)
Deploy over the top. Frontend only.

FIXED
-----
1. GRAPH NOW CHANGES PER TAB — it plots the uncategorised total for the CURRENT
   tab only (Costs / Sales / Wages), so switching tabs changes the line.
2. NO GRAPH ON OVERHEADS — the Overheads tab shows the summary only (full width).
3. PROFESSIONAL STYLING — rebuilt with recharts to match the Scorecards charts:
   proper X/Y axes, £k Y-axis formatting, tooltip, smooth line, dots.
4. NUMBERS & DATES NO LONGER CLIPPED — real axes with margins/width fix the
   out-of-view issue from the hand-drawn SVG.
5. TARGET LINE — a green dashed reference line at zero ("aim for the dashed line").
   When the purple line sits on it, everything's allocated. Shows an
   "All allocated ✓" note when there are no spikes.
6. ROBUST MONTHS FILTER — the month dropdown/filter now derives the month from the
   raw date even for old "28 Feb 2023"-format wage lines, so months appear even if
   a file hasn't been re-uploaded since the date-parsing fix.

STILL A DATA ACTION (not a code issue)
--------------------------------------
"No categorised Direct Wages" = no project-tagged wages have been uploaded yet
(diagnostic showed categorisedWageLines=0). To populate them, upload the RECENT
project-TAGGED wages export (the tracking-transfer journal style file, e.g. the
1784157057530-style one). The long 2023–2026 file is all untagged, so it only
ever fills the uncategorised side.

FILE CHANGED
------------
• pages/bookkeeping.js — recharts graph, per-tab series, target line, robust months
