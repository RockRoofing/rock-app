DEPLOY NOTES - Planner: shrink header labels so they fit their cells
====================================================================

ONE file, an EDIT - just replace it.
  pages/operations/project-management/planning.js

CHANGE
  The column header labels (Planned / Actual, Start, Contract Compl., No. of Visits) were
  spilling over their cells. They are now smaller, centred and tightly spaced so they sit
  neatly inside each column. The sticky header height was nudged slightly to fit the
  two-line labels. Nothing else changes.

BUILD: next build -> Compiled successfully.
