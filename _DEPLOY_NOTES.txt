Outstanding Invoices — default sort = Due date
==============================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully  ·  tab shows "· v9"

CHANGE
------
The page now defaults to sorting by "Due date" (soonest first) instead of
"Most overdue". The Sort dropdown still offers Most overdue / Largest due /
Due date if you want to change it.

FILES
-----
• pages/outstanding-invoices.js
