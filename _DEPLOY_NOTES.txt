DEPLOY NOTES - Tech Sub: clarify approval visibility for internal staff
=======================================================================

ONE file, an EDIT - just replace it.
  pages/design/[project]/tech-sub.js

CONTEXT
  The approval WAS still on the latest revision - it just isn't shown to Rock Roofing
  (internal) users, because only the assigned CUSTOMER can approve (as requested). Logged
  in as the customer, the Approve button appears on the latest/current revision as normal.

THIS CHANGE
  So it's not confusing for the internal team, Rock Roofing users now see a small
  "Awaiting customer approval" note where the Approve button would be (on the current,
  not-yet-approved revision) - both on the table row and in the viewer. Nothing about who
  can actually approve has changed; this is just a clearer status for internal staff.

BUILD: next build -> Compiled successfully.
