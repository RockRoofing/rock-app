DEPLOY NOTES - Project completeness: retention comments + variations not mandatory
==================================================================================

TWO files, both EDITS - just replace them.
  lib/projectComplete.js       (retention comments no longer required)
  pages/api/dashboard.js       (cache marker bumped so the banner recomputes)

CHANGES
1. "Insert retention release date comments" is now OPTIONAL. It no longer appears in the
   "still to complete" list and does not stop a project counting as complete.
2. Variations were never part of the completeness check, so adding variations is (and
   was) not mandatory - confirmed, nothing needed to change there.

The banner recomputes on the next dashboard build (cache marker bumped), so projects
that were only flagged for a missing retention comment will clear after deploy +
one refresh/sync.

BUILD: next build -> Compiled successfully.
