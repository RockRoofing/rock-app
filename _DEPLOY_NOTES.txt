DEPLOY NOTES - Approval banner + nav reorder
============================================

FOUR files, all EDITS - just replace them.
  lib/designShell.js                              (moves Handover Docs before RFIs)
  pages/design/[project]/rock-drawings.js         (approval banner)
  pages/design/[project]/calculations.js          (approval banner)
  pages/design/[project]/tech-sub.js              (approval banner)

CHANGES
1. APPROVAL BANNER at the top of Tech Sub, Rock Drawings and Calculations:
   - RED "APPROVALS NEEDED" when any current item is not yet approved.
   - GREEN "APPROVED" when every current item is approved.
   - It looks only at the CURRENT revision of each item (superseded revisions are ignored),
     and shows nothing on an empty page.

2. NAV: "Handover Docs" now sits BEFORE "RFIs" in the Design portal navigation.
   (Handover Docs stays internal-only, so customers still don't see it.)

BUILD: next build -> Compiled successfully.
