Bookkeeping — FIX Xero reconnect (invalid_scope) — CORRECTED
============================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy, then /connect -> Reconnect Xero.

ROOT CAUSE (confirmed from Xero's allowed-scopes list)
------------------------------------------------------
Xero is migrating to granular scopes (their banner: broad scopes retire Sep 2027;
new granular names apply). The reconnect failed with invalid_scope because the
connect page asked for the WRONG name for reading wages.

The correct scope for Manual Journals (wages) is:
    accounting.manualjournals.read     ← was missing / wrongly named before
(NOT accounting.transactions.read — that's not in this app's allowed list.)

Also confirmed accounting.reports.profitandloss.read IS valid (it's in the list),
so that stays.

FINAL SCOPE STRING NOW REQUESTED
--------------------------------
openid offline_access accounting.invoices.read accounting.contacts.read
accounting.reports.profitandloss.read accounting.settings.read
accounting.manualjournals.read projects.read

Every one of these appears in the app's allowed-scopes list, so invalid_scope
should be gone.

AFTER DEPLOYING
---------------
1. /connect -> Reconnect Xero -> should reach the consent screen now. Approve.
2. /api/xero/status -> scope should include accounting.manualjournals.read
3. Run Sync Wages -> 401 gone, journal lines pull through, wages categorise.

FILE CHANGED
------------
• pages/connect.js — request accounting.manualjournals.read (correct wages scope)
