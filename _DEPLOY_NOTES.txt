pkg735 - Build marker: prove which code is live
================================================

DEPLOY OVER pkg734. Two files. Contains pkg734's fix as well, so it is safe
whether or not 734 landed.

REPLACE:
  pages/api/business-financials.js
  pages/business-financials/cashflow.js

WHY
---
Pressing "Sync invoices from Xero" changed the cash flow. That sync writes
invoiced:lines: and deletes dashboard:cache. It does NOT touch
bank:outstanding-receivables.

pkg733/734 made the cash flow read ONLY bank:outstanding-receivables. If that
code were live, that button could not have changed the forecast.

It did. So the old dashboard:cache branch is still running. Either the packages
did not reach main, or the deploy did not take. I am not going to keep guessing,
so the page now says.

WHAT YOU WILL SEE
-----------------
A blue bar at the top of the 13-week cash flow:

  pkg735 - receivables from bank:outstanding-receivables - N rows in the
  forecast, M in the store (T typed) - store synced <date> - dashboard cache
  warm/cold

If instead you get a RED bar saying "Old build still live", the API is not
pkg735 and the deploy has not landed. That is the thing to chase before any
more code changes.

READ THE NUMBERS
----------------
  "M in the store" = 0
      bank:outstanding-receivables is EMPTY. This is what I now expect. Nothing
      in the app fills it - the Bills page has a sync button, invoices never had
      one. The dashboard:cache branch masked that completely.
      -> pkg736 adds the missing sync.

  "M in the store" > 0 but "N rows in the forecast" = 0
      The store has data and the mapping is dropping it. Send me the typed
      count and I will fix the mapping.

  Both > 0 and the figures look right
      pkg734 landed after all and we are done with this thread.

NOTHING ELSE CHANGED. This is a diagnostic, not a fix. Delete the bar once this
has settled.

ABOUT THE SWING
---------------
-500k to +500k is the signature of the dashboard:cache path: 793 rows in
__UNASSIGNED__ carrying about 10m of historic value, with frozen amountDue
figures that were never refreshed because sync-invoices preserves any line dated
before its window. Bills and paid invoices from years back all landing in
"Overdue - brought forward". It is not a calculation fault; it is the wrong
source.
