ISSUES — submit fix + Required Resolution Date column + portal Add Issue

SUBMIT BUG FIX (the "Unexpected end of JSON input" error):
  middleware.js        ROOT CAUSE: on the Site App subdomain (siteapp./forms.), /api/issues and
                       /api/issue-notify were NOT in the passthrough list, so the login middleware
                       redirected them to /forms (HTML) — the form got HTML instead of JSON.
                       FIX: added /api/issues and /api/issue-notify to the Site App passthrough.
  pages/api/issues.js  Wrapped the POST handler in try/catch so it ALWAYS returns JSON (even on
                       error) instead of an empty response. Companion-submission stays best-effort.

NEW:
  pages/operations/project-management/issues.js
   - New "Required Resolution" date column, placed BEFORE Resolved Date. Editable inline and in the
     edit window. Shows a red "⚠ Overdue" note if the required date has passed and it's not resolved.
   - New "+ Add Issue" button on the tracker header so issues can be raised from the PORTAL too
     (not just the Site App). Same fields as the Site App form (project, issue name, multi-select
     type + Other, description, required resolution date, photos). Fires the CM/Ops/QS notification.
     "Created By" = the logged-in portal user's name.

Deploy over the previous versions from the issues-system package. (middleware.js is at repo root.)
