DEPLOY NOTES - Give Management access to Design Customers
=========================================================

THREE files, ALL EDITS. Replace each in GitHub.

  EDIT  pages/admin/design-customers.js   (page now allows management, back-link adapts)
  EDIT  pages/api/design-customers.js      (API now allows management)
  EDIT  pages/design/index.js              (adds a "Manage Customers" link in the Design header)

WHAT CHANGED
- The Design Customers section (managing external customer / design-team logins) was ADMIN ONLY.
  It now also allows users with MANAGEMENT permissions - both the page and its API.
- Because management users don't have the Admin area, they previously had no way to reach it.
  There is now a "Manage Customers" link in the Design portal header, shown only to admin and
  management (not to customers/external users, and not to other internal roles).
- The back-link on the page points to Design for management users, and Admin for admins.

Admins are unaffected - they still reach it from the Admin page as before.

NO vercel.json change. No cron. No middleware change. BUILD: Compiled successfully.
