Commercial cards — Under-Target-Margin card, EOM order swap, remove Budget retention
====================================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully

CHANGES (pages/commercial.js — top summary cards)
-------------------------------------------------
1. NEW "Projects Under Target Margin (< X%)" card — BOTH modes, placed just before
   "Remaining to Claim (Ex. Retention)".
   • Adjustable threshold: type a % right in the card label (default 25). Editing
     it doesn't trigger the card's click (won't toggle the filter).
   • Clickable: click the card to filter the table to only projects under target;
     click again (or the number input aside) to clear.
   • Counts/filters by the margin relevant to the view: EOM uses the selected
     month's valuation margin; Budget Tracker uses the live current margin. £0/
     un-invoiced projects (no margin yet) are not counted.

2. EOM Report: "Remaining to Claim (Ex. Retention)" and the "WIP" card are SWAPPED
   — WIP now shows first, then Remaining (Ex.), then Remaining (Inc.).

3. Budget Tracker: "Retention Outstanding" card REMOVED. Order is now: …Under
   Target, Remaining (Ex.), Remaining (Inc.).

Card grid now sizes to the number of cards (7 in Budget Tracker, 8 in EOM).

FILE CHANGED
------------
• pages/commercial.js
