PLANNING — current-week + today markers, past-editing note

  pages/operations/project-management/planning.js
   1) THICK BLUE LINE down the LEFT edge of the current week's Monday cell (start of the W/C we're in).
   2) THIN DARK-GREEN lines on BOTH the left and right edges of TODAY's cell.
   3) Both added to the colour key.
   4) Past days ARE editable — they're just only visible when "Historic" is on (now last 2 weeks).
      Footer help text updated to say so. Select past cells -> Edit labour allocation -> (optionally set
      Actual) -> Save. No code change was blocking past edits; the markers make "today" obvious now.

Deploy over previous planning.js. No API change.
