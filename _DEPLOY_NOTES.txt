DEPLOY NOTES - Add "Tasks" tab to the commercial nav on all pages
================================================================

SIX files, all EDITS - just replace them.
  pages/commercial-scorecard.js
  pages/application-calendar.js
  pages/variations.js
  pages/retention.js
  pages/outstanding-invoices.js
  pages/commercial.js

WHY
  These six pages each have their own copy of the commercial top-nav (they don't use the
  shared nav component), so the new "Tasks" tab wasn't showing on them.

CHANGE
  Added the "Tasks" link (-> /commercial-objectives) to the nav on each of these pages,
  after "Commercial Scorecard", matching the existing style. Tasks is now reachable from
  every commercial page.

BUILD: next build -> Compiled successfully.
