DEPLOY NOTES - Commercial sub-nav: reorder + Ops styling
========================================================

ONE file, an EDIT - just replace it.
  components/CommercialNav.js

CHANGES
1. Scorecards sub-nav order is now: Weekly Tasks | Monthly Tasks | Commercial Scorecard
   (Commercial Scorecard moved to the end).
2. Both sub-navs (Scorecards and Applications) now match the Operations sub-nav style:
   white bar with a grey bottom border, grey tab text, and the active tab in dark text
   with a gold underline (same as the Ops second-row tabs).

BUILD: next build -> Compiled successfully.
