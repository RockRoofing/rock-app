pkg791 - Show the 612 working instead of inferring it
======================================================

DEPLOY OVER pkg790 (contains pkg790). Fourteen files.

FOLDER NOTE: pages/project/[id].js uses LITERAL square brackets.

REPLACE:
  pages/api/dashboard.js            (changed here)
  pages/retention.js                (changed here)
  everything else as pkg790

WHY THIS AND NOT A FIFTH FIX
-----------------------------
Four attempts at the 612 figures have been made by inferring from totals. Every
one of them was a reasonable theory and none of them worked, because I cannot
see what the app actually holds. Guessing again would waste another round.

WHAT IT ADDS
------------
Click the 612 Deducted cell on any row. You get every account-612 line the app
holds for that project:

  Date | Reference | Invoice or Credit note | Raw | Used | Side | Netted | Allocs

  Raw     what came from Xero
  Used    after any credit note cancellation
  Side    which column it landed in
  Netted  highlighted amber where a reversal was cancelled
  Allocs  how many Xero allocations the line carries

With totals underneath, and Retention Owed shown for comparison.

WHAT TO DO
----------
Open J147 and read it against the spreadsheet Vinay sent. One of these will be
true and each points somewhere different:

  Twelve lines, Allocs all 0        allocations still are not syncing; the
                                    reference fallback is the route and I will
                                    see from the references why it did not match

  The credit note is missing        it never reached this project - a tracking
                                    or matching problem at sync time

  It is there with Netted blank     the pairing rule is not firing and the Raw
                                    amounts will show why

  Lines you do not recognise        something from another project is landing
                                    here

Send me a screenshot of that modal and the next fix will be the right one.
