DEPLOY NOTES - Contracted Rates: "not locked" banner
====================================================

TWO files, both EDITS - just replace them.
  pages/contracted-rates.js    (new banner)
  pages/api/dashboard.js       (exposes each project's lock status)

CHANGES
  The Contracted Rates page now shows a banner at the top (same style as the project
  details banner) listing every live project whose contracted rates are NOT locked.
  - Each project is a clickable link that jumps straight to it on the page.
  - Projects with rates uploaded but not locked show normally; projects with no rates
    yet are marked "(none)".
  - When every project is locked, it turns green: "All projects have contracted rates
    locked".
  - Locking/unlocking a project updates the banner immediately (no reload needed).

  (Negotiated/Pipedrive deals are not included in the banner - only live projects.)

NOTE: the lock status comes from the dashboard cache, which is refreshed by a cache
marker bump, so it updates after deploy + one refresh/sync.

BUILD: next build -> Compiled successfully.
