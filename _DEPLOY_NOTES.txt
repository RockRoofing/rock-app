DEPLOY NOTES - Cash Flow: delete forecasted applications
========================================================

ONE file, an EDIT - just replace it.
  pages/project-cashflow.js

CHANGE
  You can now delete a forecasted application easily:
   - Click "View" on a forecast band (or open one), then use the new red
     "Delete this forecast" button in the modal header. It removes the forecast and
     closes the modal.
   - The existing per-row "Delete" in the "View forecasts" list still works too.
  Either way the gantt bands and daily/weekly totals refresh immediately.
  A confirmation prompt guards against accidental deletion.

BUILD: next build -> Compiled successfully.
