PLANNING — HOTFIX for client-side crash

  pages/operations/project-management/planning.js
   - Fix: the new "clearing" useState had been placed AFTER the component's early loading return, which
     breaks React's Rules of Hooks and caused "Application error: a client-side exception has occurred".
   - Moved it up with the other useState calls. No behaviour change otherwise — the Clear labour button and
     save-to-clear from the previous drop work as intended.

Deploy this planning.js over the previous (broken) one.
