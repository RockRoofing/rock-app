DEPLOY NOTES - Bills: find the last £736 (reconciliation export)
===============================================================

WHERE YOU ARE
-------------
App "All outstanding bills" 257,686 vs Xero 256,950.14 - a 736 gap, with BOTH showing
103 bills. Good match on count; small value gap.

IT IS NOT ROUNDING
-------------------
The app totals sum the EXACT pence (amountDue is full precision); only the DISPLAY is
rounded to whole pounds by the gbp() formatter. So the 736 is a real data difference,
not rounding. Most likely a stale part-paid bill: e.g. Xero shows Island Scaffolding
SI-1407 as Paid 18,448.08 / Due 9,881.92 - if that payment landed after your last app
sync, the app would still show more due on that line. Xero is live; the app is a
snapshot from the last "Sync bills".

WHAT I ADDED (to find it precisely, not guess)
----------------------------------------------
1) "Export CSV" button (top right) - downloads the app's bill list with EXACT pence
   amounts, sorted by due date, including credit notes and a total. Diff this against
   your Xero CSV to see the exact line(s) that differ.
2) The "All outstanding bills" card now shows the exact figure to the penny in its
   sub-label (e.g. "103 bills - exact 257,686.00"), so you can compare precisely.

HOW TO CLOSE THE GAP
--------------------
  1. Deploy, hard-refresh, click "Sync bills" FIRST (in case the difference is just a
     payment recorded in Xero since your last sync - this alone may fix it).
  2. If still off: click "Export CSV" here and compare to your Xero export. The one or
     two lines that differ will jump out (usually a part-paid bill with a different
     "Due" figure). Send me those lines and I'll confirm the cause.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/bills.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh -> Sync bills -> (if needed)
Export CSV and diff.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
