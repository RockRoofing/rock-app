SITE APP — Drawings/RAMS in-app viewer with prev/next

  pages/forms/index.js
   - Tapping "View" (or the thumbnail) on a Drawing or RAMS now opens an IN-APP full-screen viewer instead
     of navigating away to the file link — so operatives can always get back to the app (× or Escape).
   - The viewer supports moving through the list:
        • on-screen ‹ / › arrows either side of the file,
        • "‹ Previous" / "Next ›" buttons at the bottom,
        • left/right keyboard arrows,
        • swipe left = next, swipe right = previous (touch).
   - Shows "n of N" position; Download is still available from the top bar.
   - Images render inline; PDFs render in an embedded frame within the overlay (no page navigation).

   (This file also still contains the earlier Deliveries home card and the Start-on-Site list fix, so it
    supersedes the index.js from siteapp-1b-deliveries — deploy this one.)

Deploy this single file (it replaces pages/forms/index.js). No API/cron/env change.
