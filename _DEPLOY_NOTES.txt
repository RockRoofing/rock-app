REPORT LINK — renamed + repositioned everywhere, and SRAT filter version check

RENAME: "Report a problem with the app" -> "Report an App improvement" (Portal + Site App).

POSITIONING (no more overlap):
  pages/index.js (Portal home) — link now sits to the LEFT of "Admin" in the top bar.
  components/PreContractNav.js — link on the right of the nav (Sales, Scorecards, Project Financials pages)
    — pushed right, left of any page controls, no overlap.
  pages/commercial.js — link added to the LEFT of the Budget Tracker / Upload / Xero / Sync controls.
  components/OperationsNav.js — text renamed (already sat left of "Open Site App").
  components/ReportProblemButton.js — now MODAL-ONLY (no floating button). Every area has its own inline
    link that opens the shared modal via the 'open-report-problem' event, so nothing overlaps anymore.
  pages/forms/index.js (Site App) — link/title renamed to "Report an App improvement".

SRAT FILTER (portal):
  pages/operations/project-management/srat.js — the Project / From / To filter bar IS present and
    unconditionally rendered (confirmed in code). To prove which version is live, the page title now reads
    "SRATs · filter v2".
    AFTER DEPLOY: open Operations > Project Management > SRAT.
      • If the title says "SRATs · filter v2", the new file is live and the Project + From + To filter bar
        sits directly under the title (above the list).
      • If it still says just "SRATs", the deploy didn't take — the file on GitHub isn't updating or Vercel
        served a cached build (hard refresh / confirm the deployment finished / check the file path is
        pages/operations/project-management/srat.js, NOT srats.js which is only a redirect).

Deploy all files. No cron/env change.
