Commercial — move sync buttons into the main nav; Xero connection -> Admin
=========================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully

REQUIRES the earlier "Commercial_sync_buttons_and_stamps" deploy to be in first
(it added components/SyncBar.js, /api/sync-status, sync-bills:at). This deploy
updates SyncBar and moves it into the navs. If deploying together, take THIS
SyncBar.js (it's newer — adds the compact nav layout).

CHANGES
-------
Project Financials (commercial.js — Budget Tracker & EOM):
 • The new sync buttons (Sync Invoices / Sync Wages / Upload Bills) now live in
   the MAIN NAV, replacing the old red "Sync Xero" button.
 • "Report app improvement" stays to their LEFT (unchanged position).
 • The "Xero" connection link is REMOVED from this nav (moved to Admin — below).
 • A small "synced <when>" stamp shows next to the buttons; hover shows per-type
   detail. Syncing force-refreshes the dashboard so figures update immediately.
 • (The old red button ran a legacy bills-costing sync via /api/xero/sync — now
   superseded by the new Invoices/Wages/Bills flow. Its function syncXero() is
   left in the file but unused; harmless.)

Admin centre (admin.js):
 • Added a "Xero Connection" tab (-> /connect) to the admin sub-nav, so the Xero
   connect/reauthorise link now lives in Admin.

Retention (retention.js):
 • Sync Invoices button moved into the header, BETWEEN "Report app improvement"
   (left) and "+ Add Manual Entry" (right).

Outstanding Invoices (outstanding-invoices.js):
 • Sync Invoices button moved into the header, to the RIGHT of "Report app
   improvement".

SyncBar (components/SyncBar.js):
 • New compact=true (default) inline layout for nav bars: buttons + small stamp,
   full per-type detail on hover. compact=false keeps the stacked layout with a
   full stamp row (used where there's room).

FILES
-----
• components/SyncBar.js
• pages/commercial.js
• pages/retention.js
• pages/outstanding-invoices.js
• pages/admin.js
