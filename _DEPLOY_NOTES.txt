OPERATIONS SCORECARDS — layout matched to pre-contract (full-width graphs + bottom table)

pages/operations/scorecards.js  (updated)
  - Dropped the "current month" column. Each metric is now a single FULL-WIDTH card with the label/value/
    target on the left and the 12-month trend graph spanning the rest of the width.
  - Added the RAG Key legend (On target / Close (≥80%) / Below target) next to the date filter, like
    pre-contract.
  - Added the bottom TREND TABLE (same as pre-contract): Metric | Target | one column per month, with
    RAG-coloured values and inline-editable targets. Sticky Metric + Target columns and sticky header.
  - Point-in-time metrics (Gross margin, Procurement) keep a single value card (no graph) but still appear
    as a row in the trend table.

Deploy this file. No cron/env change. (Depends on the ops-scorecards API from the previous drop.)
