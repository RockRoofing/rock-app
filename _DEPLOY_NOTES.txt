DEPLOY NOTES - TEMP diagnostic: trace where J203 is coming from
===============================================================

ONE file, NEW. Add via "Add file" at pages/api/planning-trace.js.
This is a TEMPORARY diagnostic - we'll delete it once we've found the answer.

HOW TO USE
After deploying, while logged in to the portal, open this URL in your browser:

  https://app.rockroofing.co.uk/api/planning-trace?q=J203

It returns JSON showing:
  - allocationKeysFound : the exact key(s) in the planner data that reference J203
  - projectExists / projectStatus / projectName : whether a J203 project exists and its status
  - detail[].dates : EVERY date J203 is allocated on, and which operatives (opIds) are on it

Copy the JSON back to me and I'll tell you exactly where J203 is coming from and the right fix
(e.g. it's an orphaned/test allocation to clear, or a completed project that should be hidden
from the report).

You can also check other things by changing the query, e.g. ?q=L: to list all project keys.

NO vercel.json change. No cron. BUILD: Compiled successfully.
