Retention Tracker — PC Type as Main/Sub dropdown
================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single file: pages/retention.js

CHANGE
------
The "PC Type (Main/Sub)" field in the Retention Tracker's manual entry form is now a
dropdown with the same standard options as Edit Project Details:
  — Select —
  Main PC
  Sub PC
(Was a free-text box.) Because the add and edit forms share the same component, both
now use the dropdown. Stored as pcType, unchanged.

FILES
-----
• pages/retention.js

NOTE: existing entries with free-text PC Type values that don't exactly match
"Main PC"/"Sub PC" will show as "— Select —" until re-picked (the stored value is
preserved unless changed).
