DEPLOY NOTES - Invoices Owed: tick totals, supplier filter, two charts
======================================================================

CHANGES
-------
1) TICK BOXES on each invoice row + a "Selected" total at the top (next to Total due).
   Tick rows to tally them up; header checkbox selects/clears all shown rows.
2) SORTABLE COLUMNS (INV No, Ref, To, Date, Due date, Paid, Due) - click a header to
   sort, click again to reverse. Already default-sorted by DUE DATE, OLDEST FIRST.
3) SUPPLIER (customer) FILTER: type-ahead search, multi-select. Empty = all customers.
   "Clear" / "Select shown" helpers.
4) TWO CHARTS side by side:
   - LEFT "Invoices falling due by month (all)": everything falling due in the date/
     outstanding view - NOT affected by the customer filter or row ticks (stays "all").
   - RIGHT: shows ticked rows if any are ticked; otherwise follows the customer + date
     filters. Title says which mode it's in. (Blue = right, green = left.)

Everything else (columns, Expected date, shared comments, full width) unchanged.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/invoices.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh.

VERIFY
------
  - Tick a few rows -> "Selected" total updates and the RIGHT chart switches to just
    those rows; the LEFT chart stays as all invoices due.
  - Type in the customer box -> list narrows; pick several -> table + right chart follow;
    left chart unchanged.
  - Click headers to sort; default is due date oldest-first.

BUILD
-----
Validated with `next build` -> "Compiled successfully". Hooks confirmed above the early
return.
