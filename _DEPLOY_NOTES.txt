GANTT BATCH (part 1 of 2) — searchable installer dropdown + "Programme dates needed" warning

pages/operations/project-management/planning.js

1) SEARCHABLE OPERATIVE DROPDOWN when adding labour
   - Both the Allocate modal and the Water Ingress day modal now use a type-ahead search box instead of a
     long <select>. Start typing a name (or company) and the matching installers filter down; click one to
     add. Still shows the "⚠ no CSCS/WAH" flag, and "+ Add new operative to the roster" is unchanged.

2) "PROJECT PROGRAMME DATES NEEDED!" warning
   - On the Gantt, any project that has labour allocated but NO programme start/completion dates now shows a
     red "⚠ Project Programme dates needed!" line under the project name (same style as "⚠ No supervisor").
     (The small ⚠ prefix was there before; this makes it explicit, as requested.)

Deploy this file. No cron/env change.

STILL TO COME (Gantt batch part 2):
  - Copy/paste cells (select gang -> Copy -> paste to another project/week)
  - RAMS-not-signed warning (flag allocating an operative not on the project's RAMS Matrix)
Plus later: automated weekly labour email to accounts, Water Ingress "missing form" logic, Meeting Actions
carry-over.
