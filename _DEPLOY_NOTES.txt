Applications — picker Remove button + Summary tidy
==================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully  ·  Applications tab shows "· v14"
(Page-only change.)

1) REMOVE BUTTON IN THE MATERIALS POPOUT
   Added lines now show "✓ Added" with a red "Remove" button right there in the
   "Add materials on site" popout — so an accidental add can be undone without
   leaving the window. Removing the last line of a PO also removes its (now empty)
   supplier group header. The line becomes addable again immediately.

2) SUMMARY — CONTRACT SUM COLUMN TIDIED
   In the Summary block, the Contract Sum figures next to "Variations" and
   "Materials On Site" have been removed (left blank). Only "Measured Work" carries
   a Contract Sum. (The Materials On Site "Projected Final Account" cell is also
   left blank, as before.)

FILES
-----
• pages/applications.js   (v14)
