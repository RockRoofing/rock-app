Commercial cards/headers + Income chart tweaks
==============================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully

1. Remaining to Claim CARDS relabelled (Budget Tracker + EOM):
   • "Remaining to Claim (Inc. WIP, Ex. Retention)"
   • "Remaining to Claim (Inc. WIP, Inc. Retention)"
   (Values already include WIP; labels now say so.)

2. Budget Tracker TABLE HEADERS: added hover descriptions to the Labour,
   Materials and Total Budget columns (previously only Contract group had them).
   All data columns now have tooltips.

3. Project Details → Income tab: monthly bar chart is now ALL GREEN (removed the
   dark "latest month" bar) and the key no longer shows a "Latest month" entry.

FILES CHANGED
-------------
• pages/commercial.js
• pages/project/[id].js  (latest full version)
