DEPLOY NOTES - Status counts banner (Rock Drawings + Calculations)
==================================================================

TWO files, both EDITS - just replace them.
  pages/design/[project]/rock-drawings.js
  pages/design/[project]/calculations.js

CHANGE
  A second banner now sits under the approval banner on Rock Drawings and Calculations:

    Status: X for Review / Comment  -  X Construction Issue

  - "for Review / Comment" = number of current items not yet approved.
  - "Construction Issue" = number of current items marked Construction Issue.
  - Counts cover the CURRENT revision of each item only (superseded revisions are ignored),
    and the banner is hidden when the page is empty.

BUILD: next build -> Compiled successfully.
