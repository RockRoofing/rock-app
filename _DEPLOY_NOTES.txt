PLANNING — WLA send/download refinements

  pages/operations/project-management/planning.js
   - "Send to operatives" now becomes "✓ Sent" (green, disabled) after a successful send. To send again,
     close the pop-out and hit "Send Weekly Labour Allocation" again (reopening resets it).
   - "Download PDF (this week)" renamed to just "Download PDF"; it now downloads ALL selected weeks
     (matches the "how far ahead" filter), passing &weeks=N.

  pages/api/planning-week-pdf.js
   - Accepts ?weeks=N and assembles N consecutive weeks.

  lib/weeklyLabourPdf.js
   - buildWeeklyLabourPDF now takes { weeks:[...] } and renders EACH WEEK ON ITS OWN PAGE.
   - Row height increased (24 -> 34) so the company line under each name is no longer cut off.
   - Unnamed slots render as "N unnamed" in the PDF too.

Deploy over previous. No env / cron change.
