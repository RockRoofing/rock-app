SITE APP + PLANNING — fixes batch 1

CLIENT-SIDE CRASH FIXES
  pages/forms/index.js
   - FIX: Drawings/RAMS "View" threw "application error / client-side exception". Cause: isImage() was
     defined inside ProjectDetailsView but used by the separate FileViewer component (out of scope).
     isImage is now a module-level helper. Drawings & RAMS View works again.
  pages/operations/project-management/planning.js
   - FIX: Planning WEEK view threw a client-side exception. Cause: the Water Ingress row's week total did
     `g.days.reduce(...)` but week groups are plain arrays (g), so g.days was undefined. Now `g.reduce(...)`.

VARIATIONS
  pages/forms/cm/variations.js
   - FIX: no variations were showing. Now reads the full project record via /api/ops-projects?no=<projectNo>
     (settings.variations) instead of the dashboard cache. Project-first, then Variation number,
     Description, Instructed, Total (as specified).

DELIVERIES
  pages/forms/deliveries.js
   - Proof-photo thumbnails now load via the inline image proxy so they render (and tapping opens the
     full in-app viewer with prev/next). (Attachment viewing inside the app now works.)

LIVE TASKS
  pages/forms/cm/tasks.js
   - Target-completion dates now use the SAME colour coding as the portal (red = due today/overdue,
     orange = within a week, green = >1 week away).

SRATs
  pages/forms/cm/srats.js
   - Can now EDIT existing SRATs from the app (Edit button on each card).
   - Timeline box moved BELOW Actions and is now the same size as the other boxes (full textarea).
   - Can add Live Project Tasks from the SRAT form — they're created in Live Tasks and linked to the SRAT.
   - Linked Live Project Tasks now show on the SRAT card (with status + due date).

HOME BADGES
  pages/forms/index.js
   - Issues Log card shows an ORANGE count of issues needing actioning (your projects).
   - Live Tasks card shows a RED count of overdue tasks (your projects).
   - Pre-Start Notification form is now hidden from the operative "Complete a Form" list (it lives in the
     CM Pre-Start area — full customer-send rework to follow).

Deploy all files. No cron/env change.

STILL TO COME (next drop — larger features):
  1. Pre-Start area rework: project-first; choose "Complete new" or "View completed" (table of dates ->
     view portal of the completed PSN); form changes to "Submit & send to customer", pulling customer
     contacts from the IHM/Project Details with add/delete.
  2. Forms: view/edit completed forms from inside the Site App (completed-forms browser).
  3. Missing Forms "pre-start not required" wording tidy-up (verify after Pre-Start rework).
