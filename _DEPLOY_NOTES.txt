PLANNING — Gantt fixes

  pages/operations/project-management/planning.js
   1) FROZEN COLUMN: only the PROJECT column is now frozen when scrolling horizontally. The Start and
      Contracted Completion columns scroll with the calendar (they were wrongly frozen before).
   2) WEEKEND ALLOCATION: weekends can now be selected/dragged and have labour allocated (still shaded
      grey so they read as weekends, but fully clickable). Rare, but supported.
   3) WEEK VIEW NUMBERS: the number now counts ALL days worked in the week including weekends over a
      5-day base — e.g. 6 days worked shows "6/5", 7 shows "7/5" — while the coloured bar is capped at
      the 5/5 (100%) width so a full week and a weekend-worked week look the same length.

Deploy over the previous planning.js. No API change.
