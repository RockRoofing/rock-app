DEPLOY NOTES - Cash Flow: View button + adjustable period on forecasts
======================================================================

ONE file, an EDIT - just replace it.
  pages/project-cashflow.js

CHANGES
1. VIEW BUTTON on each forecast band: every coloured application band on the gantt
   now has a small "View" button (top-right). Click it to open that specific
   forecasted application to view or edit it.

2. ADJUSTABLE PERIOD: the forecast modal's period (from / to dates) is now editable
   at the top - change them and the band moves/resizes on the gantt when you save.
   - Opening via "View" loads that application's own values and period and saves back
     IN PLACE ("Update forecast").
   - Building from a fresh selection still creates a NEW forecast as before.
   Cumulative maths correctly uses the application dated immediately before the
   period, and excludes the one you're editing.

BUILD: next build -> Compiled successfully.
