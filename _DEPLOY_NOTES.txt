DEPLOY NOTES - Always-on diagnostic for "Average Days Beyond Terms"
===================================================================

WHY
---
You reported no amber line. That means either the diagnostic build wasn't live, or
the amber line's condition (only shown when zero qualify) hid it. This build makes
the diagnostic ALWAYS visible under the card, so there is no ambiguity.

WHAT YOU'LL SEE UNDER THE CARD
------------------------------
One small line, always present, one of:

  1. Grey: "Of 812 invoice lines: 240 have a paid-on date, 700 have a due date,
     300 read as paid, 180 qualify."  -> data is fine; if 180 qualify the card
     value should show. 

  2. Amber: same counts but "0 qualify" - tells us exactly which gate fails
     (no paid-on date / no due date / not read as paid). If it says
     "No paid-on dates in the data (Xero list omits FullyPaidOnDate)", that's the
     answer to your question: the paid date is NOT in the synced data and we need
     the payments-date fallback (see below).

  3. RED: "Diagnostic not present - this build is not deployed yet."
     -> the earlier build never went live. Deploying THIS file fixes that.

FILE (single EDIT - no new files)
---------------------------------
  - pages/commercial-scorecard.js

DEPLOY
------
Replace the file. Commit -> Vercel auto-deploys. Hard-refresh the Scorecard.

WHAT I NEED BACK
----------------
Just tell me the numbers from that one line. Specifically:
  - how many have a paid-on date
  - how many qualify
That tells me definitively whether Xero is giving us the paid date at all.

LIKELY NEXT STEP (I'll build once we see the numbers)
----------------------------------------------------
If "0 have a paid-on date": Xero's Invoices LIST endpoint isn't returning
FullyPaidOnDate for your org. The fix is a PAYMENTS-based sync - pull the Xero
Payments endpoint (bulk, dated, links to invoice IDs) and derive each invoice's
paid date from its payment. That is reliable regardless of the list quirk. I did
NOT build it yet to avoid guessing - the diagnostic will confirm it's needed first.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
