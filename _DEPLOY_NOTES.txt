pkg491 - the 2nd retention release cancelled itself out
=======================================================

Deploy after pkg490.

CHANGED FILES (replace)
-----------------------
  lib/applications.js
  lib/applicationPdf.js
  pages/applications.js
  pages/api/applications.js

NEW FILES: none.


NO, IT IS NOT RIGHT - AND YOU FOUND IT FROM THE ONE FIGURE THAT GAVE IT AWAY
------------------------------------------------------------------------------
    Retention released    Current £1,962.68   Previously £1,962.68   This cert £0.00

Current is the position on the job SO FAR. Two halves have been released by this point, so
it should read £3,925.37. It showed one half - and cancelled against the previous column,
so the certificate claimed nothing.

The cause: "released" was read from THIS application's tick boxes only. App 3 has the 2nd
half ticked and not the 1st, so Current counted one half; Previously counted the 1st from
app 2; the difference was zero.

Released to date is now cumulative - this application's halves OR any released earlier.

    current    £3,925.37
    previously £1,962.68
    this cert  £1,962.68     <- the 2nd release, correctly claimed

Checked against your actual figures.


AND YOUR SUGGESTION, WHICH IS THE RIGHT ONE
---------------------------------------------
Both halves now appear in the summary once either has been released:

    1st Release Retention (released previously)    £1,962.68
    2nd Release Retention                          £1,962.68

A certificate headed "2nd Retention Release" with no sign the first ever happened invites
the obvious question from the customer. Now it answers it.

Same on screen, where a half released earlier is labelled and its tick box locked.


ALSO FIXED, THE UNDERLYING CAUSE
----------------------------------
A new application did not inherit the release flags from the one before it. pkg473 assumed
a claimed half stayed ticked and locked - nothing was carrying it forward, so it appeared
un-claimed and could have been claimed a second time.

Now carried forward at creation. The cumulative reading above means your EXISTING
applications are already correct without it - I verified that on app 3 as it stands - but
the flags being right matters for what the tick boxes show.
