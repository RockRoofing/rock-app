DEPLOY NOTES - Variations: don't repeat the project number in the name column
=============================================================================

ONE file, an EDIT - just replace it.
  pages/variations.js

CHANGE (display only - nothing else touched)
  The "Project Name" column no longer repeats the project number that's already shown
  in the "Project No" column. If a project's name starts with its job number (e.g.
  "J203 - Hollis Croft"), the name column now shows just "Hollis Croft". Names that
  don't include the number are shown unchanged.

  The PROJECT FILTER dropdown still shows "J203 - Hollis Croft" (number kept) so the
  list stays ordered by number and projects remain easy to find.

  This is purely how the name is displayed in that one cell - the stored data, the Xero
  name, the filters, the edit modal and every other page are untouched, so nothing that
  links to this sheet is affected.

BUILD: next build -> Compiled successfully.
