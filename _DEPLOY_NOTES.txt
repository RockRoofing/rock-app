PROJECT CONCERNS v2 — banner fix
  components/ProjectConcerns.js
   - Banner now reflects ONLY the most recently added meeting (older meetings'
     next-dates are ignored, so a stray old date can't hijack the banner).
   - Timezone-safe date handling: dates display and compare as the exact calendar
     date entered (no day-shifting).
