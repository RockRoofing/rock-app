DEPLOY NOTES - O&M: each Tech Sub as its own section
====================================================

ONE file, an EDIT - just replace it.
  pages/api/design-oms.js

CHANGE
  When a project has multiple Tech Subs, each one is now its OWN section in the O&M Manual:
    - Each gets its own separator / cover sheet ("Tech Sub 1 - <title>", "Tech Sub 2 - ...").
    - Each is listed separately on the contents page with its own page number.
  (Only the current, non-superseded revision of each Tech Sub is included, as before.)

  Drawings, Calculations, Leak Test Certs and Warranties are unchanged - each stays a single
  combined section.

  The readiness warning still flags when there are no Tech Subs at all.

REMEMBER: rebuild the O&M Manual to regenerate the PDF.

BUILD: next build -> Compiled successfully.
