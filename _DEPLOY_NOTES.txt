SITE APP — Deliveries card warning wording

  pages/forms/deliveries.js
   - On each delivery card, when no attachment is present the warning now reads:
       "⚠ No proof of delivery attached. Delivery needs confirming arrived."
     (bold, orange, wraps cleanly; "Update ›" stays on the right). When attachments exist it shows the
     attachment count in green as before.

  NOTE: this file is the FULL enhancements version of pages/forms/deliveries.js — it also contains the
  Required delivery date + comments on the card, bulleted line items, the reworded save warning, and the
  "not delivered OR due within 2 weeks" filter. Deploy THIS file as pages/forms/deliveries.js (it
  supersedes both siteapp-1b-deliveries and deliveries-enhancements versions).

Deploy this single file. No API/cron/env change.
