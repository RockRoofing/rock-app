PLANNING — supervisor flag rework

  pages/operations/project-management/planning.js
   - "⚠ No supervisor" (project name column) now clears if a qualified supervisor is EITHER assigned in
     Project Details OR allocated on the Gantt for that project. It still only appears when the project has
     NAMED labour (never with no labour or only unnamed labour).
   - Removed the per-day bright-red "no supervisor" cell shading entirely (and its colour-key entry).
   - Project name column widened (220 -> 280) and "⚠ No supervisor" now sits on its own line under the
     project name so it's always fully visible.

Deploy over previous planning.js. No API change.
