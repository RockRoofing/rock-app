"REPORT AN APP IMPROVEMENT" on the commercial sub-pages + SRAT marker removed

  pages/retention.js            — report link added to the nav (left of "+ Add Manual Entry").
  pages/variations.js           — report link added to the nav (left of "+ Add Variation").
  pages/application-calendar.js — report link added to the nav (pushed to the right).
  pages/commercial-scorecard.js — report link added to the nav (right side).
  All four are orange "⚠ Report an App improvement" and open the same report popup (via the global
  'open-report-problem' listener already deployed in _app.js / ReportProblemButton).

  pages/operations/project-management/srat.js — removed the temporary "· filter v2" marker from the title
  (now just "SRATs") since the filter is confirmed working.

Deploy all files. No cron/env change.
