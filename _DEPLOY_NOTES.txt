REPORT A PROBLEM — positioning + styling fix, and admin table update

PROBLEM
  The global fixed button was overlapping the "Open Site App ↗" link on Operations pages.

FIX
  components/OperationsNav.js
   - Added "⚠ Report a problem with the app" (orange text, warning sign) immediately to the LEFT of the
     "Open Site App ↗" link, in the nav row. It opens the same report popup. The Site App link is
     untouched and still links to the Site App.

  components/ReportProblemButton.js
   - No longer draws a floating button on Operations pages (the nav link handles those) or the Site App.
   - On other portal pages (home, pre-contract, commercial, admin) it shows a small ORANGE
     "⚠ Report a problem with the app" link top-right (no background box) — no overlap.
   - Both triggers open the SAME modal (nav link fires an 'open-report-problem' event the component
     listens for).

  pages/admin/problem-reports.js
   - Table now has a STATUS column (Open / Resolved). Default view is OPEN, so resolved issues disappear
     from view (switch Status to Resolved/All to see them).
   - Filters at the top are USER and STATUS.
   - "Mark resolved / Reopen" per row.

Deploy all files. No cron/env change.
