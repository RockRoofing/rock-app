DEPLOY NOTES - Project details completeness (full): 0% retention, contacts, comments
====================================================================================

FOUR files, all EDITS - just replace them.
  lib/projectComplete.js       (completeness rules)
  pages/api/dashboard.js       (passes resolved team/contacts + fixes retention 0)
  pages/project/[id].js        (retention comments box relabelled)
  pages/retention.js           (tooltip wording matched)

This REPLACES the earlier "retention zero" package - deploy these instead (superset).

WHAT A PROJECT NOW NEEDS TO COUNT AS COMPLETE (banner clears when all present):
  - Application day, Valuation day, Payment day
  - Contract value, Labour budget, Materials budget   (0 is accepted as a real value)
  - Retention %   (0% is accepted - no longer wrongly flagged)
  - Retention release date comments   (NEW - required)
  - PC date (or TBC), Defects date (or TBC)
  - ALL Rock Roofing team roles: Contracts Manager, Operations Manager, Quantity
    Surveyor, Estimator   (resolved from the IHM or an override)
  - At least ONE customer contact (with a name)

KEY FIXES
  1. 0% retention no longer flags "incomplete" - it's a valid answer. (Same fix applied
     so a genuine 0 in the financial boxes isn't treated as blank.)
  2. Rock team + customer contact now count toward completeness, as requested.
  3. "Retention comments" box renamed to "Insert retention release date comments".

NOTE: the banner recomputes on next dashboard build (a cache marker forces this). After
deploy it may take one refresh/sync to update. Some existing projects will now correctly
show incomplete where a team role, customer contact or retention comment is missing -
that's intended.

BUILD: next build -> Compiled successfully.
