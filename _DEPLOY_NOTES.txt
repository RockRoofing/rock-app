DEPLOY NOTES - Fix: "Forgot password?" did nothing
===================================================

ONE file, EDIT. Replace in GitHub.

  EDIT  pages/login.js

PROBLEM
Clicking "Forgot password?" on the main login did nothing. The forgot-password form was
accidentally nested inside the wrong branch of the page logic, so it only showed when a user
was ALREADY in the "must reset password" state - never from the normal login screen.

FIX
The "Forgot password?" view is now checked first, so clicking the link switches straight to the
"enter your email -> send reset link" form. Everything else (the API, the reset link email, and
the /reset-password page) was already correct - this was purely the login page not showing the
form.

(No change needed to the other files from the Forgot-password package.)

NO vercel.json change. No cron. BUILD: Compiled successfully.
