pkg391 - logo only in the CRM nav
=================================

Deploy after pkg390. One changed file: pages/crm.js. No new files.

The words "Rock Roofing" are gone from the left of the CRM toolbar. The logo stays, same
size and position, with the same spacing to the Pre-Contract button beside it.

Note the /crm-scores page (the one-off project scores tool) still shows the logo with the
name next to it. Left as it is - it is a standalone admin page rather than part of the CRM
toolbar. Say the word if you want that matching too.
