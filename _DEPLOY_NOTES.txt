DEPLOY NOTES - Sales: per-month reconciliation strip (to pinpoint May)
=====================================================================

May still shows the wrong figure, so I need to see EXACTLY what it's made of rather than
guess. This adds a reconciliation strip that appears when you click a month.

WHAT TO DO
----------
1. Deploy this file. Hard-refresh Sales. (No sync needed - it reads existing data.)
2. Click the MAY bar (or the May row via the month).
3. A yellow strip appears above the table showing:
     "May 26 reconciliation: lines total <X> (<n> lines) vs P&L <Y> (diff <Z>)
      - by source: Invoice: <a> | Manual journal: <b> | Credit note: <c> | Bank receipt: <d>"

SEND ME THAT STRIP. It tells us instantly which it is:
  - If one SOURCE is negative when it should be positive (or vice-versa) -> a sign issue
    on that source only.
  - If "Manual journal" is roughly DOUBLE the expected WIP -> double counting.
  - If the P&L diff is large and one source is missing -> a missing transaction type.
  - If lines total is the NEGATIVE of the right number -> a total-level sign flip.

Right now the app shows May = -102,521 while Xero = +92,417. The visible rows appeared to
sum to +102,522 (same size, opposite sign), which this strip will confirm or correct by
showing the true per-source breakdown and line count.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/sales.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh -> click May -> send the strip.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
