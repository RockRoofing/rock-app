pkg652 - FIX: periodMonths is not defined
==========================================

*** SUPERSEDES pkg591 - pkg651. ***
*** FIVE NEW FILES - create these in GitHub:
      pages/business-financials/forecast-balance-sheet.js
      pages/business-financials/forecast-pl.js
      pages/business-financials/forecast-margin.js
      pages/business-financials/balance-sheet.js
      components/PageErrorBoundary.js
***


CHANGED FILES (replace)
-----------------------
  pages/project-cashflow.js       <- the fix
  pages/applications.js
  pages/business-financials/cashflow.js
  pages/api/business-financials.js
  pages/api/project-cashflow.js
  components/BizNav.js
  pages/_app.js
  pages/business-financials.js
  pages/business-financials/invoice-finance.js
  pages/api/dashboard.js
  pages/connect.js
  lib/xero.js


MY BUG, AND THE SAME SHAPE AS THE LAST ONE
-------------------------------------------
The "names the missing months" message I added in pkg651 sits in TermEditor - a
SEPARATE component. periodMonths lives in HypAppModal and is invisible there.
Passed in as a prop.

AND A SECOND ONE BEHIND IT. The same message also used appCalendar, which is
HypAppModal's name for an object TermEditor already receives as `calendar`. It
would have thrown the moment the missing-months branch ran - so fixing only the
first would have bought one more round trip.

That is now FIVE times this session that a component has reached for a value
belonging to another scope. My per-component scan only compared each component
against module scope; it did not check whether an identifier belonged to a
DIFFERENT component. It does now - that cross-component check is what found the
appCalendar one, and it comes up clean on this file.


ALSO IN THIS PACKAGE (pkg651)
-----------------------------
THE MONTHLY DATE TABLE COUNTS AS AN APPLICATION CALENDAR. appCalendarUsable only
looked at the three default day fields, so J190 - blank defaults, fifteen months
of overrides filled in - read as having no calendar. The forecast fell back to one
date at period end and the applications screen warned about days you had
deliberately overridden.

    before  usable false -> one date at period end
    after   Sep val 30/09 cash 23/10, Oct 31/10 cash 27/11, Nov 30/11 cash 25/12

So one application spread over several months now gives one application date per
month at that month's percentage.
