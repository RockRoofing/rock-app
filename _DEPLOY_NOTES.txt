DEPLOY NOTES - Procurement Savings merged into Schedule + IHM attachments
=========================================================================
*** This REPLACES the earlier v1 / v2 packages - deploy THIS one (it contains everything).
    You have not deployed yet, so just use these five files. ***

FIVE files, all EDITS (no new files). Replace each one.

  pages/api/procurement.js                              (IHM -> schedule sync, incl attachments)
  pages/operations/project-management/procurement.js    (schedule columns + savings + gating)
  pages/handover/form.js                                (IHM procurement fields + attachments)
  pages/operations/projects.js                          (removes Procurement Savings tab)
  components/PreContractNav.js                          (removes Procurement Savings nav)

WHAT'S INCLUDED
1. Procurement Schedule: new columns before "Order Placed?":
     Budget Total | Budget Comments | Buying Total | Buying Comments | Total Savings
   Total Savings = Budget Total - Buying Total (a saving is POSITIVE and shows GREEN;
   an overspend is negative and shows RED). Order Placed moved to the end of the row;
   Attachments stay at the end. Order can't be placed until the four savings fields are
   filled in.

2. Internal Handover Minutes: each procurement item has the four savings fields AND an
   Attachments control. When the minutes are completed, the savings fields AND any
   attachments copy through to the Procurement Schedule automatically (one-time sync;
   afterwards the live Schedule is master). The attachment is then viewable on the
   Schedule page's Attachments (paperclip) for that row.

3. Procurement Savings tabs removed from Pre-Contract nav and the Projects per-project tabs.

BUILD: next build -> Compiled successfully.
