Application PDF — column fix + standalone sections + section totals
==================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single-file change on top of Phase 4 (pages/API unchanged).

FIXES
-----
1) COLUMN OVERRUN FIXED
   Contract Works columns were overlapping (Rate printing over Total, headers like
   "TToottaal"). Re-spaced with fixed, non-overlapping right-aligned money columns:
   Code | Description (wraps) | Qty | Unit | Rate | Total | To date. Descriptions
   now wrap on word boundaries (no mid-word breaks).

2) VARIATIONS & MATERIALS ON THEIR OWN PAGES
   Each of these sections now starts on a fresh page.

3) SECTION TOTALS
   A bold total row at the bottom of each section:
     • TOTAL CONTRACT WORKS      (Total + To date)
     • TOTAL VARIATIONS          (Final value + To date)
     • TOTAL MATERIALS ON SITE   (Total + To date)
   (Verified: the J178 Contract Works "To date" total comes to £55,335.38, matching
   the Summary's Measured Work figure.)

FILES
-----
• lib/applicationPdf.js   (only file changed)
