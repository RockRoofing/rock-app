pkg710 - The overheads: TWO IMPLEMENTATIONS of the same rule
=============================================================

*** SUPERSEDES pkg591 - pkg709. ***
*** SEVEN NEW FILES - create these in GitHub (as before) ***


CHANGED FILES (replace)
-----------------------
  pages/api/business-financials.js
  ... and the rest of the set as before


HIDDEN ROWS WAS NOT IT EITHER - THE GAPS DID NOT MOVE
-------------------------------------------------------
Identical to before: 11,408 in Aug and Sept, 951 in Oct and Nov. So removing the
28 hidden accounts changed nothing, which means they are empty - and your own
Reconcile line said so: "All: 767,312 | Shown: 767,311 - matches".

That was my fifth wrong answer on this. So this time I stopped guessing at
candidates and read what the Budgets page ACTUALLY does.


IT DOES NOT USE THE API'S FIGURES AT ALL
------------------------------------------
    Budgets page:  forecastOf(code, mo), its own baseForecast(), its own isComplete()
    API:           computePredictedByCodeMonth(), its own baseForecastOf(), isCompleteMo()

TWO SEPARATE IMPLEMENTATIONS OF ONE RULE. They will always drift, and they had:

    Budgets page   isComplete    = actualSet.has(mo) && availableSet.has(mo)
    API            isCompleteMo  = mo < nowKey && availableSet.has(mo)

The API also required the month to be IN THE PAST. August is in the past and is
not switched to actual, so the two tests disagree on exactly that month - and the
averaging methods ("last 3 months", "avg this FY") then draw on a DIFFERENT SET OF
MONTHS on each side, which is why the gap changes between September and October.

The API now uses the identical test. Same rule, same months, same answer.


A TDZ CAUGHT IN BUILD
---------------------
isCompleteMo now reads actualSet, which was declared four lines below it. Compiles
cleanly, throws on the first call. Moved.


IF THIS ONE IS STILL OUT, tell me and I will make the Forecast P&L read the
Budgets page's own total rather than recomputing it. That is the only way to
guarantee they agree, and after six attempts it is the right call - I should
probably have done it three packages ago.
