DEPLOY NOTES - Payless Notices: 0 every month but 8 total - window/diagnostic fix
=================================================================================

THE CONTRADICTION
-----------------
"8 total across all months" but every month shows 0 and the average is 0. That means
the 8 credit notes ARE being found and counted (total), but they aren't landing in
the months the line graph was looking at. The most likely reason: the trend was tied
to the page's DATE-RANGE FILTER (dateFrom..dateTo), and the 8 credit notes are dated
in months outside that window (or the window didn't line up).

THE FIX
-------
1. The Payless line graph now uses a FIXED last-12-completed-months window (same as
   Gross Margin), independent of the date-range filter. So the months always line up
   with where the credit notes actually are.
2. Added a diagnostic line under the card that lists WHICH months hold credit notes,
   e.g. "8 total across all months shown. By month: Mar 26: 2, Apr 26: 3, ...". If it
   says "None fall in the last 12 months (older credit notes, or a date issue)", then
   the 8 are older than 12 months or have a date problem - and we'll know instantly.

FILE (single EDIT - no new files)
---------------------------------
  - pages/commercial-scorecard.js

DEPLOY
------
Replace the file. Commit -> wait for Vercel "Ready" -> HARD-refresh the Scorecard.

WHAT TO CHECK / SEND BACK
-------------------------
Look at the diagnostic line under Payless Notices:
  - "By month: <Mon>: N, ..."  -> those months should now show bars/points on the
    line graph. If they do, we're done.
  - "None fall in the last 12 months ..."  -> the 8 credit notes are older than 12
    months (or mis-dated). Tell me and I'll widen the window or check the dates.

NOTE: still confirm the Average Days card shows "(v3)" so we know the build is live
(from the previous notes) - if it doesn't, the deploy isn't taking effect and that
must be resolved first, as it affects this card too.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
