PLANNING — PHASE 4 of 5: Weekly labour pop-out (PDF + per-operative email)

NEW FILES
  lib/weeklyLabourPdf.js            Two PDF builders: the full week grid (landscape) and a single
                                    operative's own week (portrait, for the email attachment).
  pages/api/planning-week.js        Assembles one week BY OPERATIVE: rows of {name,email,phone,company,
                                    cells[7]} + daily totals. Only operatives allocated that week appear.
  pages/api/planning-week-pdf.js    GET ?monday=YYYY-MM-DD -> the week grid PDF.
  pages/api/planning-week-email.js  POST { monday } -> emails EACH operative their OWN week (HTML + a
                                    one-page PDF attachment). Operatives without an email are skipped
                                    and reported back.

CHANGED
  pages/operations/project-management/planning.js
    - The W/C week headers are now CLICKABLE (👥 in day view). Click a week to open the Weekly Labour
      Allocation pop-out: a per-operative grid (operatives down the side, Mon–Sun across, project in
      each cell, weekends shaded, daily total installers row) — mirroring your spreadsheet.
    - Buttons: Download PDF, and "Email each operative their week" (sends each person their own week).

Requires Resend env already set (RESEND_API_KEY + FORMS_FROM_EMAIL) — same as issues/report emails.
Deploy over previous. Uses Phase 1 roster (for names/emails) + Phase 2 allocations.

NEXT: Phase 5 = Forms split (Completed | Missing), driven by the Gantt allocations.
