DEPLOY NOTES - Minutes: rich-text Scope + auto-growing text boxes
=================================================================

FIVE files, all EDITS - just replace them.
  components/opsUI.js              (new AutoTextarea + RichText components)
  pages/handover/form.js          (IHM: Scope rich text + auto-grow boxes)
  pages/operations/projects.js    (renders the Scope rich text read-only)
  components/PreStartForm.js       (auto-grow boxes)
  components/ProjectConcerns.js    (auto-grow boxes)

1. SCOPE OF WORKS - RICH TEXT (Internal Handover Minutes)
   The "Scope of works" box now has Bold / Italic / Underline buttons. Formatting is
   saved and shown (formatted) on the project's Handover view. Existing plain-text
   scopes still display fine.

2. AUTO-GROWING TEXT BOXES (IHM, Pre-Start, Project Concern)
   Multi-line entry boxes now GROW DOWNWARDS as you type, so long text wraps and stays
   readable instead of scrolling sideways in a short fixed box. Applied to the long-
   text / comments / description / mitigation boxes across all three minutes forms.

BUILD: next build -> Compiled successfully.
