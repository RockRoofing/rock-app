FORMS — MISSING — date filter diagnostics

The From/To week filter code is correct (From W/C … To W/C … -> refetches on change, API builds the week
range from those). I couldn't find a code bug, so this adds visibility to pin down what's happening:

pages/operations/forms-missing.js
  - Title now shows "Forms — Missing · v2" so we can confirm the new file is actually deployed.
  - Next to the To selector it now shows the ACTIVE RANGE and result count, e.g.
      "Showing W/C 30 Jun 2025 → W/C 14 Jul 2025 · 12 rows"  (or "· loading…").
    Change the From/To dropdowns and this line + the row count should update immediately.

AFTER DEPLOY, please check:
  1. Does the title say "· v2"?  If NOT -> it's a deploy/cache issue (old file still live); hard-refresh or
     confirm the Vercel deploy finished.
  2. If it says v2: change the From / To week dropdowns and watch the "Showing … · N rows" line. Does the
     range text change? Does the row count change?
  - Tell me what you see (v2 or not, and whether the count moves) and I'll know exactly where the problem is.

NOTE: the filter is WEEK-COMMENCING dropdowns (not free date pickers). If you expected to type exact dates,
that's the difference — say the word and I can switch them to date inputs.

Deploy this file. No cron/env change.
