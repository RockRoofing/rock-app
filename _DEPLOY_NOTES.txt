DEPLOY NOTES - Payless Notices now per-month (not all-time total)
================================================================

WHAT CHANGED
------------
The Payless Notices card headline was showing the ALL-TIME total of credit notes.
It now shows the LATEST completed month's count, and the line graph shows the count
PER MONTH with its trend line (the per-month line was already there; the headline
was the only all-time figure).

  - Headline = number of credit notes (payless notices) in the last full month.
  - Sub-label shows that month and its count, e.g. "Credit notes per month - Jun 26: 3".
  - Trend line = one point per month (each month's count) with the dashed trend line.
  - Small grey line still shows the total across all months displayed, for reference.
  - RAG dot: lower-is-better (fewer payless notices = green; target defaults to 0).
  - Click the card: the per-month drill (raw count + editable adjusted count + the
    credit notes behind each month) is unchanged.

NOTE: the per-month counts and manual adjustments are unchanged - this is a display
change to the headline, using data the API already returns (paylessCountByMonth).

ALSO INCLUDED (cumulative in the scorecard file)
------------------------------------------------
This commercial-scorecard.js also carries the earlier, not-yet-confirmed changes:
  - "Average Days Beyond Terms (v3)" version marker + the headline display fix
    (reads paymentDiag.overallAvgDaysBeyondTerms; shows "Avg = N days").
So deploying this supersedes the previous AvgDaysBeyondTerms bundle - you only need
these two files.

FILES (both EDITS - no new files)
---------------------------------
  - pages/api/commercial-metrics.js     (unchanged from last bundle; include to be safe)
  - pages/commercial-scorecard.js       (per-month payless + v3 marker + avg-days fix)

DEPLOY
------
Replace both files. Commit -> wait for Vercel "Ready" -> hard-refresh the Scorecard.

VERIFY
------
  - Payless Notices headline = last month's credit-note count (not a big all-time
    number); the line graph shows a point per month with a trend line.
  - While here, confirm the Average Days card title reads "(v3)" - that tells us the
    build is actually live (see previous notes).

BUILD
-----
Validated with `next build` -> "Compiled successfully".
