pkg634 - The bar now shows the real figure, and its tooltip shows the record
=============================================================================

*** SUPERSEDES pkg591 - pkg633. ***
*** ONE NEW FILE - create it in GitHub:
      pages/business-financials/balance-sheet.js
***


CHANGED FILES (replace)
-----------------------
  pages/project-cashflow.js
  pages/business-financials.js
  components/BizNav.js
  lib/xero.js
  pages/api/business-financials.js
  pages/api/dashboard.js
  pages/connect.js
  pages/business-financials/cashflow.js
  pages/business-financials/invoice-finance.js

NEW FILE (create)
-----------------
  pages/business-financials/balance-sheet.js


YOU WERE RIGHT AND MY EXPLANATION WAS WRONG
-------------------------------------------
"Press Save" was not the answer - you had saved, the override persisted, and
save() calls setHypApps() so the timeline updates immediately. I also checked
gbpK: 38,125.84 renders as "£38k", so the formatter is not hiding it either.

So the record genuinely holds ZERO in revenueThisPeriod while holding the right
labour (4,000) and materials (18,000) - which is why the bar reads Rev £0 next to
Lab £4.0k and Mat £18k.

The likely cause remains a save made while the prior was still wrong, when the
increment was negative and this clamped it to nil:

    const revenueThisPeriod = revOverride == null ? revenueCalculated : Math.max(0, ...)

But I have guessed at this chain several times now, so rather than a fifth theory
this package makes the record itself visible.


1. THE BAR FALLS BACK TO THE CERTIFICATE VALUE
-----------------------------------------------
Where revenueThisPeriod is missing or zero but thisCertTotal is not, the bar shows
thisCertTotal and marks it with an asterisk:

    stored 0, cert 38,125.84    ->  £38k*
    stored 38,125.84            ->  £38k
    genuinely nil period        ->  £0

So a stored zero can no longer masquerade as a nil period, and the timeline stops
disagreeing with the modal.


2. HOVER THE BAR TO SEE WHAT IS STORED
--------------------------------------
    Stored on this forecast:
    revenueThisPeriod   £0.00
    thisCertTotal       £38,125.84
    grossClaimedToDate  £137,066.06
    prevGrossOverride   £97,761.07
    revenueOverride     (none)
    saved 30/08/2026, 21:14

That settles which field is wrong and when it was written, without another round
trip. Hover it and send me the values if the asterisk is showing.


ALSO IN THIS PACKAGE
--------------------
pkg633 - warns in the modal when the saved figure and the live one differ.
pkg632 - a real APPLICATION is never filtered out of the prior by the period
dates. That is the -160k, and the reason Labour this period showed the whole
budget on J190.
pkg627-631 - Balance Sheet tab and Financing & tax column, Reference column, date
pickers that commit on blur, nav order, Summary using the shared nav.
