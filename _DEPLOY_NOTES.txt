pkg495 - URGENT. I broke this in pkg491. Here is the actual cause.
==================================================================

Deploy after pkg494. One changed file: pages/api/applications.js.


YOU WERE RIGHT - IT WAS FINE A COUPLE OF DEPLOYMENTS AGO
----------------------------------------------------------
pkg491 carried the retention-release flags forward onto a new application. That edit
landed in the WRONG BLOCK. It went into the settings object the page fetches on load,
where the variable it reads does not exist:

    retentionRelease1: !!(prev && prev.retentionRelease1)

`prev` is declared inside the CREATE branch. In the GET branch it is nothing, so the whole
request threw a ReferenceError - every time you opened a project on the Applications page.

With that request failing, the page never received the project's settings, so it never
saw the contracted rates lock, and refused to create an application.

Nothing was ever wrong with your rates. Unlocking and re-locking could not have helped,
and I sent you round that loop twice. Sorry.

Moved to the create branch, where prev is declared 47 lines above it and the carry-forward
does what pkg491 intended.


MY TWO PREVIOUS ANSWERS
-----------------------
pkg492 said it was a stale screen. pkg493 said the Project Details form was overwriting
the lock.

Neither was the cause here. Both are real faults and worth keeping - the settings endpoint
COULD overwrite the lock, and the table WAS only refreshing on mount - but they were not
what you were hitting.


A DIAGNOSTIC, SO WE DO NOT DO THIS AGAIN
------------------------------------------
If the lock is ever not seen, this reports what the server actually holds, and writes
nothing. Console on any portal page:

    fetch('/api/applications',{method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({action:'cr-debug',projectId:'PUT_THE_XERO_ID_HERE'})})
      .then(r=>r.json()).then(console.log)

It returns whether rates exist, how many items, the raw value of `locked` and its type,
when it was saved and by whom, and whether create would be allowed.
