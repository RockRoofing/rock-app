Financial App — Outstanding Invoices: add "Report app improvement" button
=========================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Replaces the outstanding-invoices page (deploy over the top).

CHANGE
------
The "⚠ Report app improvement" button was missing from the Outstanding Invoices
nav. Added it to the top-right, matching the other commercial pages. (The modal
itself is already mounted globally in _app.js, so the button now opens it.)

FILE CHANGED
------------
• pages/outstanding-invoices.js
