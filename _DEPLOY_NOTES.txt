ATTACHMENTS — View now renders exactly like the Drawings viewer

  Why it kept failing: the viewer was loading images through the /api/download proxy and had an aggressive
  <img onError> fallback that hid the image and showed "Download to view" whenever anything went slightly
  wrong. The Drawings viewer (which works) simply uses a plain <img src={file.url}> against the blob URL
  with no proxy and no error fallback.

  components/RowAttachments.js
   - Viewer now renders images with a plain <img src={f.url}> and PDFs with <iframe src={f.url}> — the
     exact same approach as the working Drawings viewer. Removed the proxy src and the onError "Download to
     view" fallback that was swallowing the image.
   - Still portalled to document.body (so it can't be clipped by the table) and still has prev/next.
   - Download button unchanged (proper filename via proxy).

  NOTE: the /api/download inline mode from the previous drop is no longer needed by the viewer; you can
  keep the earlier download.js as-is (harmless) — no need to redeploy it.

Affects Deliveries, Procurement, Risk Log, Live Tasks (shared component). Deploy this single file.
