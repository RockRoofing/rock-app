GANTT — layout tidy + sticky selection bar

pages/operations/project-management/planning.js

  - "Planning" heading moved to the TOP of the section, on the same row as the view controls.
  - The right-side controls (Send Weekly Labour Allocation, View Weekly Labour Allocations, Day/Week,
    Historic, ‹ Today ›) are now up TOP next to the heading — no longer sat above the gantt.
  - Removed the subtitle text ("Project programme — installers per project per day…").
  - Filters row (Project / Installer / From / To / Clear) tightened up; gap between the filters and the
    gantt/labour-edit section closed.
  - SELECTION ACTION BAR IS NOW STICKY: when you select cells (on any project, including ones low down the
    list), the yellow "… days selected / Allocate labour / Edit / Clear / Deselect" bar sticks to the top
    of the screen and stays visible while you scroll — no more selecting on a lower project then scrolling
    back up to use the buttons.

NOTE: this file supersedes all previous gantt drops (searchable dropdown, warning rework, historic scroll,
RAMS warning) — it contains everything. Deploy just this one.

Deploy this file. No cron/env change.

STILL TO COME (gantt): copy/paste cells.
