Retention register — match indicators + sortable columns
========================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single file: pages/retention.js (latest — includes all prior retention changes)

1) RETENTION OWED vs 612 MATCH COLUMN (left of "612 Allocated")
   • Green tick ✓ when Retention Owed == 612 Allocated (within £1).
   • Red flag 🚩 when they differ.
   • Grey — when neither has a value yet.

2) APPLIED-FOR vs INVOICED MATCH COLUMN (right of "Applied for")
   • Green tick ✓ when Applied for == Invoiced (within £1).
   • Red flag 🚩 when they differ.
   • Grey — when either is missing.

3) DEFAULT SORT BY REF
   The register loads sorted by Ref (job number), ascending.

4) CLICK-TO-SORT HEADERS
   Click a column header to sort by it; click again to reverse. An arrow (▲/▼) shows
   the active sort column and direction. Sortable: Ref, Customer, Project, Final
   Account, Applied for, Invoiced, Retention Owed, 612 Allocated, Ret %, PC Type, QS,
   1st Date, 2nd Date, Total Paid. (Match/among-derived columns aren't sortable.)

FILES
-----
• pages/retention.js

REMINDER: the 612 Allocated figures (and therefore the Owed-vs-612 match) only
populate after an Invoices re-sync.
