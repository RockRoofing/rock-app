pkg535 - the instruction page: no suggested names, no half-filled form
======================================================================

Deploy after pkg534.

CHANGED FILES (replace)
-----------------------
  components/VariationBuilder.js
  pages/instruct/[token].js          <- literal [token] folder

NEW FILES: none.


1. REAL NAMES WERE THE PLACEHOLDERS
-------------------------------------
The boxes read "Jack" and "Belshaw" in grey - my placeholder text from pkg527. On a form
whose entire purpose is recording WHO is instructing, a suggested name is the last thing
to put in front of somebody.

Now "First name...", "Last name...", "Your role...", "Your company...".


2. THE FORM CONTRADICTED ITSELF
---------------------------------
Your screenshot says "We do not hold your details for this project, so please enter them"
- and the company box was already filled from the project.

The prefill ran regardless of whether we actually held the person. Now it only runs when
we do:

    SENT TO A HANDOVER CONTACT     details shown as FIXED TEXT, not inputs, and the
                                   server uses its own copy whatever is posted
    SENT TO A TYPED ADDRESS        all four boxes empty, nothing suggested

Your test went to a gmail address that is not on the handover, which is why you got the
entry form - that part was right. It just should not have arrived part-filled.


3. "REQUESTED BY -" ON THE DOCUMENT
-------------------------------------
Also in your screenshot. Nothing required it, so a variation could go to a customer with
that field blank.

Requested by and the variation description are now both required before it can be raised.
A variation nobody asked for is one a QS will not pay for, and "Requested by -" is the
first thing they query.


STILL OUTSTANDING
-----------------
Anyone holding the To recipient's own link can use it. That is the one-time code we
discussed - they click, we email a six-digit code to that address, they enter it. Say the
word.
