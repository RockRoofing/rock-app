DEPLOY NOTES - Consolidate into "View Weekly Labour Allocations" (+ overheads on report)
========================================================================================

TWO files, BOTH EDITS. Replace both in GitHub.
(This supersedes the separate "Accounts Weekly Labour" packages - everything is now folded into
"View Weekly Labour Allocations". Deploy the earlier Overheads + O/A API packages first, i.e.
pages/api/planning.js and pages/api/planning-accounts-pdf.js must already be live.)

  EDIT  pages/api/planning-week.js                        (overheads now appear in the week data)
  EDIT  pages/operations/project-management/planning.js   (removed the Accounts button; View is now the hub)

WHAT CHANGED
1. The separate "Accounts Weekly Labour Allocation" BUTTON is REMOVED.
2. "View Weekly Labour Allocations" is now the single place for everything:
   - Pick a Week commencing (2 years back to 1 year forward) and how many weeks.
   - See each staff member's row with their actual allocation in each day cell.
   - OVERHEADS now show in the cells too - e.g. "Holidays", "Paid Leave", "Sick" - on that
     person's own row, exactly like a project (shown in purple).
   - Click any day cell to toggle that person's OVERNIGHT ALLOWANCE (O/A). It SAVES instantly
     and the "O/A" tag appears on the cell. Go back to any past/future week and the saved O/As
     are shown again.
   - "Review past O/A" tab: filter all saved O/As by date range + installer, and remove entries.
   - "Download report" produces the PDF (installers + overheads in the cells, O/A marked).

NOTE: overhead people appear on their own row on the day(s) they have an overhead. Someone with
only overheads that week still gets a row.

NO vercel.json change. No cron. BUILD: Compiled successfully.
