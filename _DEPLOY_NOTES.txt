SITE APP — STAGE 1b: Operative Deliveries + Start-on-Site fix

  pages/forms/index.js
   - FIX: "Start on Site Checklist" is no longer forced under the Contracts Manager section — it's a normal
     form completable by anyone and now sits in the main alphabetical list. (The CM section now only holds
     forms explicitly tagged accessLevel:'contracts-manager', i.e. the H&S Accident & Incident Report.)
   - Added a "Deliveries" card to the Site App home (all users).

  pages/forms/deliveries.js  (NEW) — operative Deliveries schedule:
   - Select a PROJECT first (only projects that have deliveries are listed); the project name shows at the
     top once selected.
   - Per delivery shows: PO No, Supplier, Items, required/actual date, comments, attachment status.
   - Tap a delivery to update. EDITABLE on mobile: Actual Delivery Date, Comments, Attachments only.
     Everything else (PO, supplier, items, required date) is read-only.
   - Proof of delivery = a photo (camera or gallery) OR a typed delivery note. If none is attached, a
     warning appears on save ("Save anyway") — the operative can still submit.
   - NO delete option.
   - Reads/writes the SAME ops:deliveries store as the portal Deliveries page (Xero-synced POs), so
     confirmations/photos/comments appear in both.

  middleware.js
   - Added /api/deliveries, /api/operatives and /api/planning to the Site App passthrough so the Deliveries
     view (and the Daily Site Diary Gantt auto-fill from 1a) can load their data.

Deploy all three. Deploy AFTER siteapp-1a-forms (this index.js supersedes 1a's index.js).
No cron/env change.
