DEPLOY NOTES - Bookkeeping weekly tasks: Thursday
=================================================

TWO files, both EDITS - just replace them.
  pages/bookkeeping-weekly-tasks.js   (weekly columns now Thursday)
  lib/notifications.js                 (bookkeeping notification anchor -> Thursday)

CHANGE
  The Weekly Bookkeeping Tasks now run to a THURSDAY (was Friday) - columns are each
  week's Thursday date, and the subtitle reads "...by the end of play each Thursday."
  Tracking start set to 30 Jul 2026 (the Thursday of that week). The notification
  scheduler's bookkeeping week anchor is moved to Thursday too, so any bookkeeping
  weekly notifications reference the correct week.

NOTE: any weekly bookkeeping Yes/No already ticked was stored under a Friday key and will
show blank under the new Thursday columns (the data isn't lost, just under old keys).
As this was only just set up there is likely little/no data to re-enter. Monthly tasks
are unaffected.

BUILD: next build -> Compiled successfully.
