Admin — fix missing nav tabs on Portal Users & Templates
========================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy over the top.

CAUSE
-----
Portal Users (pages/admin.js) and Templates (pages/admin/templates.js) have their
OWN hardcoded nav arrays (they don't use the shared AdminShell). Those arrays were
stale — missing "Account Categorisation" and "Xero Upload" — so the full menu
didn't show on those two pages.

FIX
---
Both inline nav arrays now match the full Admin tab list, in order:
Portal Users · Templates · Form Builder · Site App Users · Documents ·
RAMS Director · Account Categorisation · Xero Upload · App Improvements
(Both nav bars already scroll horizontally, so the longer menu isn't clipped.)

FILES CHANGED
-------------
• pages/admin.js            — full nav tab list
• pages/admin/templates.js  — full nav tab list
