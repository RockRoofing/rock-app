PROJECT REPORT — issue & photo rule refinements

PHOTOS:
  - Issue photos are only included if the linked issue was SENT to the customer (or marked sent).
  - Photos from issues marked "do not send", or with no send/do-not-send decision yet, are EXCLUDED.
  - Non-issue form photos are unaffected (all included, since last report).

ISSUES TABLE + APPENDED FORMS (new logic):
  - Include issues with OPEN or CLOSED status (previously open-only) that have been SENT to the customer.
  - Only issues created since the last report for this project.
  - EXCLUDE any issue already included in a previous report (each issue appears on one report only).
  - Status column now shows Open/Closed correctly.

  pages/operations/project-management/report.js   All of the above applied in the project auto-pull.

(PDF endpoint + PDF table already filter to sent issues and to the report's snapshot, so they stay
consistent with the new rule. Deploy over the previous version; pairs with report-issues-sent-only.)
