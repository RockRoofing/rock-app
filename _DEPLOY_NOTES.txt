pkg738 - Project labels on receivables (small fix)
===================================================

DEPLOY OVER pkg737. One file.

REPLACE:
  pages/api/business-financials.js

The store writes the tracking option as `project`. pkg733 read
`trackingProject`, which is not a field on those rows - so every project label
on a receivable came out blank, silently. Both names now accepted.

This is cosmetic. It does NOT explain the missing 127k or the invoice due
tomorrow.
