pkg380 - project score mapping, Actively Chased, and no more false red months
=============================================================================

Deploy after pkg379.

CHANGED FILES (replace)
-----------------------
  lib/crmImportMap.js
  lib/crmFieldSchema.js
  pages/scorecard-crm.js

NEW FILES: none.


1. PROJECT SCORE - YOU WERE RIGHT, AND I CHECKED YOUR EXPORT
------------------------------------------------------------
"Label" is column 50 of deals-2641369-41.xlsx. It was there all along. The import mapping
read 34 "Deal -" columns and Label was not one of them, so every score was dropped on the
way in.

What is actually in it, from your file:

    scored 5   92        scored 2   42
    scored 4   86        scored 7   39
    scored 3   60        scored 8   21
    scored 6   47        scored 9   16
                         scored 1   12

415 deals carry a score. Blank on the other 6,445 - which is fine, most were never scored.

Now mapped to project_score, which is the field the "Glenigan scored 5 or more" metric
reads. It applies to any FUTURE import - see the note at the bottom about getting the
existing 415 in without a wipe-and-replace.


2. LEAD SOURCE IS FINE - AND THAT MATTERS
-----------------------------------------
I checked, because if lead source had been the problem then none of the date work would
have helped. From your export:

    Glenigan                     1154
    Glenigan, Other                56
    Glenigan, Website New Build     1
                                 ----
                                 1211 deals

The metrics match on "glenigan" appearing anywhere in the value, so the comma-separated
ones are counted correctly. Lead source was never the blocker. The missing received date
was, exactly as diagnosed.

"Actively Chased" added to the Lead Source options, between Website Refurb and Other.

Worth knowing: Glenigan Project ID is populated on 4,940 of 6,860 deals - far more than
carry a Glenigan lead source. So it is NOT a reliable "this came from Glenigan" signal,
and the metrics correctly ignore it. I had suggested using it; checking the data says no.


3. NO MORE RED DOTS FOR MONTHS WE NEVER MEASURED
------------------------------------------------
Your screenshot showed twelve straight months of red on all three cards. That is not a
year of missed targets - it is a year we did not record the data.

Months before tracking began now show a dash, no rating and no red dot. The average, the
trendline and the chart all skip them.

The start date is one line at the top of pages/scorecard-crm.js:

    const GLENIGAN_TRACKING_FROM = '2026-08-01'

Set to this month. Change that one string if you ever backfill properly.


GETTING THE 415 EXISTING SCORES IN
----------------------------------
The mapping fixes future imports, but a full re-import is wipe-and-replace - and the CRM
now holds things the export does not: filed email, thread links, CRM-created activities,
notes, hand-made corrections. A re-import would take those with it.

So DO NOT re-import to get the scores.

Say the word and I will build a scores-only patch instead: upload that same xlsx, it
matches on deal ID and writes project_score onto existing deals, touching nothing else.
Perhaps thirty minutes, and no risk to anything already in the CRM.


WHAT HAPPENS FROM HERE WITHOUT ANY MORE WORK
--------------------------------------------
  Received date    recorded on every stage move made in the CRM     from today
  First value      recorded on every value edit made in the CRM     from today
  Project score    typed into the Project Score field on a deal     from today

All three metrics start filling in from this month. Before that, dashes - which is the
honest answer.
