DEPLOY NOTES - Average Days Beyond Terms: data is fine, fixing the headline display
===================================================================================

WHAT THE DIAGNOSTIC TOLD US (good news)
---------------------------------------
Your card showed: "Of 152 invoice lines: 93 have a paid-on date, 144 have a due
date, 136 read as paid, 93 QUALIFY."

So the paid dates ARE in the data and 93 invoices are being measured. This was never
an empty-data problem - the earlier fixes worked. The card just wasn't showing the
headline number cleanly (the big figure area looked blank while the small "Avg"
line showed "On terms").

THE FIX
-------
1. The API now computes ONE authoritative overall average across all 93 qualifying
   invoices (all months pooled) and returns it as
   paymentDiag.overallAvgDaysBeyondTerms.
2. The card headline now reads that single value directly, instead of re-deriving it
   from the monthly buckets (which is what caused the headline/avg mismatch).
3. The diagnostic line now also prints the computed average, e.g.
   "... 93 qualify. Avg = 2 days beyond terms." - so the number is visible and
   verifiable at a glance.

Value formatting (unchanged, already correct): positive = "+N days late",
negative = "N days early", zero = "On terms".

NOTE: if the true average really is ~0, the headline will legitimately read
"On terms" - that's a real result (on average you're paying on terms), not a bug.
The diagnostic line's "Avg = N days" now makes that explicit.

FILES (both EDITS - no new files)
---------------------------------
  - pages/api/commercial-metrics.js
  - pages/commercial-scorecard.js

DEPLOY
------
Replace both files. Commit -> Vercel auto-deploys. Hard-refresh the Scorecard.

VERIFY
------
The Average Days Beyond Terms card should now show a clear headline (e.g.
"+2 days late" or "On terms") AND the grey diagnostic line should read
"... 93 qualify. Avg = N days beyond terms." Click the card to see the per-invoice
Due Date / Paid Date / Days +/- Terms breakdown for all 93.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
