SRAT FILTER — re-check build (the date filter code is confirmed present)

WHAT'S HERE
  pages/forms/cm/srats.js — the SRATs page WITH the From/To date-range filter, plus a small "v2" marker
  next to the "SRATs" title so you can confirm the new file is actually live.

HOW TO CHECK AFTER DEPLOY
  1. Open the Site App > Contracts Manager > SRATs and PICK A PROJECT (the filter only shows once a project
     is selected — the first screen is the project picker).
  2. If you see "SRATs v2" in the title, the new file IS deployed:
       - The From / To date inputs appear directly under the "+ New SRAT" button.
  3. If you DON'T see "v2", the deploy didn't take effect — likely causes:
       - The file on GitHub didn't actually save to pages/forms/cm/srats.js, or
       - Vercel served a cached build (try a hard refresh / redeploy), or
       - You're viewing a different page (this is /forms/cm/srats, reached via the CM "SRATs" card).

This is a diagnostic drop — deploy this one file. Once we confirm "v2" shows, I'll remove the marker in a
later update.
