DEPLOY NOTES - Text-box fix, button label, email CTAs
=====================================================

FIVE files, all EDITS - just replace them.
  components/DrawingMarkup.js                    (markup text-box typing fix)
  lib/designEmail.js                             (email button label configurable)
  pages/api/design-rock-drawings.js              (emails say "Review Drawings")
  pages/api/design-techsubs.js                   (emails say "Review the Tech Sub")
  pages/design/[project]/rock-drawings.js        (button label)

CHANGES
1. Rock Drawings: the "View" button is now "View / Add Comments".

2. MARKUP TEXT BOX FIX: text typed into a mark-up text box was appearing REVERSED. Fixed at
   the source - the text box now keeps the cursor in the right place while you type. Because
   every page (RFIs, Tech Sub, Rock Drawings, and the older drawing pages) uses this same
   mark-up component, this fixes the reversed text EVERYWHERE at once.

3. EMAIL BUTTONS now name the right item:
   - Drawing emails say "Review Drawings" (was "View the RFI").
   - Tech Sub emails say "Review the Tech Sub".
   - RFI emails say "Review the RFI".

BUILD: next build -> Compiled successfully.
