Financial App — batch 4a (update): editable + deletable comments
================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Replaces the batch 4a outstanding-invoices files (deploy over the top).

CHANGE
------
Each comment in the invoice comments pop-out now has Edit and Delete controls:
• Edit — inline edit box with Save / Cancel; edited comments show "(edited)".
• Delete — removes the comment (with a confirm prompt).

FILES CHANGED
-------------
• pages/outstanding-invoices.js     — edit/delete controls + inline editor
• pages/api/outstanding-invoices.js — delete-comment action
