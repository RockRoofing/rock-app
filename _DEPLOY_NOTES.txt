PROJECT VIEW — three fixes

1) LAND ON PROJECT DETAILS
   pages/operations/projects.js
   - Opening a project now lands on the "Project Details" tab (was "Handover"). Applies to the View button,
     the project-name link, and the default.

2) EDIT + DELETE ON PROJECT FORMS (and both Forms pages stay in sync)
   lib/db.js            - new deleteSubmission(id): removes the submission key + its index entry.
   pages/api/submissions.js - new DELETE /api/submissions?id=... (edit already existed via PUT).
   pages/operations/projects.js (Project Forms tab) - each row now has "View / Edit" and a red "Delete".
   pages/operations/forms.js (main Forms page)      - each row now has "View / Edit" and a red "Delete".
   - Both pages read/write the SAME submissions store, so deleting in either place removes it from both.
     Editing uses the existing submission modal (saves via PUT).

3) DRAWINGS DELETE BUTTON CLIPPED
   components/ProjectFiles.js
   - The View / Download / Delete row on each drawing/file card now wraps (flexWrap) with a slightly
     smaller gap, so the Delete button is no longer pushed out of view on narrow cards. (Affects Drawings
     and any other ProjectFiles-based file grid.)

Deploy all five. No cron/env change.
