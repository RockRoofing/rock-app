DEPLOY NOTES - Bills to Pay: "Due in month" filter + click-a-bar to filter table
================================================================================

CHANGES
-------
1) DUE IN MONTH filter: a new dropdown in the filter bar. Pick a month to show only
   bills due in that month. "All months" clears it. The month list respects your current
   date range + supplier filter.
2) CLICK A BAR: clicking a bar on EITHER chart now filters the table to just that
   month's bills (and highlights the clicked bar). Clicking the same bar again, or
   choosing "All months", clears it. The month dropdown and the bar click are the same
   filter, so they stay in sync.

Notes:
- The LEFT chart stays "all bills" so you can always click a different month.
- The RIGHT chart follows the filters (so it narrows to the picked month too) - matches
  its "Filtered bills by month" purpose.
- Selecting a month also updates the Filtered total.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/bills.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh.

VERIFY
------
- "Due in month" dropdown lists the months that have bills; picking one filters the table.
- Click a bar on the left (red) or right (blue) chart -> table shows only that month;
  the bar highlights; Filtered total updates.
- Click the same bar again or pick "All months" -> filter clears.

BUILD
-----
Validated with `next build` -> "Compiled successfully". Hooks confirmed before the early
return.
