DEPLOY NOTES - Planning tab: no loading screen (dates + modals)
===============================================================

ONE file, EDIT. Replace in GitHub. (Supersedes the previous "Planning_no_loading_flash"
package - includes that fix too, so just deploy this one.)

  EDIT  pages/operations/project-management/planning.js

WHAT WAS STILL LOADING
Entering dates into the Planned/Actual Start and Contract Completion columns triggered the
full-screen loader, because saving a date (and the shift-start that moves the gantt) called
the full page reload via onSaveMeta={load}.

FIX
- Date saves in those columns now refresh IN PLACE (onSaveMeta -> refreshData), so the gantt
  updates instantly with no loading screen and no scroll-to-top.
- Also included from the previous package: the allocate modal and water-ingress modal now
  refresh in place too.
- The full loader now only runs ONCE, when you first open the Planning page.

Everything else is unchanged.

NO vercel.json change. No cron. BUILD: Compiled successfully.
