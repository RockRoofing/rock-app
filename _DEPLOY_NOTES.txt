Contracted Rates — Select-all now includes headings
===================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single file: pages/contracted-rates.js

The header "select all" checkbox now selects HEADINGS as well as item rows (both
sections), so you can select everything — headers included — and move it above/below
the line in one go. Struck lines stay excluded. The Above/Below selection subtotals
are unchanged (headings carry no value, so they don't affect the money totals).

NOTE: this file also contains the earlier Group A changes (searchable picker, sticky
section totals, heading checkboxes, save->lock prompt, budgets-on-lock). If you have
NOT yet deployed GroupA_v2, this single file replaces its pages/contracted-rates.js
(you still need the other GroupA_v2 files: pages/api/contracted-rates.js and the new
components/ProjectSearchSelect.js).
