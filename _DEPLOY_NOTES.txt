DEPLOY NOTES - O&M readiness warning + Construction-Issue-only
==============================================================

TWO files, both EDITS - just replace them.
  pages/api/design-oms.js
  pages/design/[project]/oms.js

CHANGES
1. ONLY CONSTRUCTION ISSUE items are included for BOTH Rock Drawings AND Calculations.
   (Tech Subs, Leak Test Certs and Warranties are unchanged.) Current drawings/calcs that
   are not yet marked Construction Issue are left out of the manual.

2. READINESS WARNING - shown in BOTH places:
   - A yellow banner on the O&M page listing anything that is missing, or any drawings /
     calculations still not marked Construction Issue (so would be left out).
   - A confirmation popup when you click Build listing the same, ending with:
     "Please check with your Rock Roofing Design Manager that the O&Ms are ready to be
     compiled. Build anyway?"
   You can still choose to build (some jobs legitimately won't have every section), but the
   warning makes sure it's a deliberate decision.

BUILD: next build -> Compiled successfully.
