DEPLOY NOTES - Commercial Objectives page + remove Retentions Invoiced card
===========================================================================

NEW FILES - add via GitHub "Add file":
  pages/api/commercial-objectives.js   (stores the Yes/No grid)
  pages/commercial-objectives.js        (the Objectives page)

EDITS - replace:
  components/CommercialNav.js     (new "Objectives" tab)
  pages/commercial-scorecard.js   (Retentions Invoiced card removed)

WHAT THIS DOES

COMMERCIAL OBJECTIVES (new tab in the Commercial nav: "Objectives")
  Two tables - Weekly Commercial Tasks and Monthly Commercial Tasks. Objectives run
  down the left, MONTHS run across the top (12 months back -> 12 months forward, 25
  columns), oldest first, current month highlighted.
  - The only value in a cell is Yes / No. Click a cell to cycle: blank -> Yes -> No ->
    blank. Each entry records who set it and when (hover a cell to see).
  - Date filter (From / To month) narrows the visible months so you can look back over
    old data to confirm tasks were done over time. Reset clears the filter.
  - Weekly table objectives (5) and Monthly table objectives (6) are exactly as you
    specified.

SCORECARD
  The "Retentions Invoiced" card has been removed from the Commercial Scorecard.

NOTE ON "WEEKLY" TABLE: the columns are MONTHS (as you specified the layout - months
across the top for both tables). So a Weekly-table cell means "kept up through that
month". If you'd rather the weekly table show one column PER WEEK instead, tell me and
I'll switch that table to weekly columns.

BUILD: next build -> Compiled successfully.
