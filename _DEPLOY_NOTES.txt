Bookkeeping — Accounts role access to tools + fix Bookkeeping hub 404
====================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Deploy over the top.

IMPORTANT — THE 404 FIX
-----------------------
The "Bookkeeping Admin" 404 was because pages/bookkeeping/admin.js wasn't in
GitHub. This package includes it. In the GitHub web editor: Add file -> Create new
file -> type the path  pages/bookkeeping/admin.js  (the "/" makes the folder) ->
paste the contents. Make sure it sits ALONGSIDE the existing pages/bookkeeping.js.

ACCESS CHANGES (as requested)
-----------------------------
The bookkeeper (Accounts role) can now use the financial tools; only WIPE stays
admin-only:
• Xero Upload            -> Accounts, Management, Admin
• Account Categorisation -> Accounts, Management, Admin
• Data Management (wipe) -> Admin ONLY (unchanged)
Enforced on BOTH the pages (AdminShell now takes an `allow` prop) and the APIs
(requireRole updated). Import APIs previously allowed 'post-contract' — now
'accounts' instead.

RELOCATED UNDER BOOKKEEPING
---------------------------
• The hub is renamed "Bookkeeping Tools" (was "Bookkeeping Admin").
• Reached from the Bookkeeping dashboard buttons (⬆ Upload Bills, ⚙ Bookkeeping
  Tools) — now shown to Accounts/Management/Admin (was admin-only).
• The hub shows Account Categorisation + Xero Upload to all allowed roles; the
  Data Management (wipe) card shows to ADMINS ONLY.
• These three pages already sit outside the general Admin nav (shown with a
  "← Bookkeeping Tools" context bar).

NOTE: the black top bar on those pages still reads "Admin" and the Data Management
tab may appear in the context bar for non-admins (clicking it redirects them, as
it's admin-only). Harmless; can tidy later if you want.

FILES CHANGED
-------------
• components/AdminShell.js               — `allow` prop for role-gating
• pages/admin/xero-upload.js             — allow accounts/management/admin
• pages/admin/account-categorisation.js  — allow accounts/management/admin
• pages/bookkeeping/admin.js       (NEW) — hub, renamed, Data Mgmt admin-only card
• pages/bookkeeping.js                   — tool buttons shown to accounts+
• pages/api/import-bills-bulk.js         — allow accounts
• pages/api/import-wages-bulk.js         — allow accounts
• pages/api/import-invoices-bulk.js      — allow accounts
• pages/api/account-categorisation.js    — allow accounts
