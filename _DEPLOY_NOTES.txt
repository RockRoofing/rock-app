DEPLOY NOTES - Business Financials "Invoices Owed": sales invoices only + columns + comments
============================================================================================

WHAT WAS WRONG
--------------
The Business Financials "Invoices Owed" page pulled from view=invoices, which was
bringing in the wrong data (costs). It now uses the SAME data source as the commercial
Outstanding Invoices page (which matches Xero):
  - Invoice lines from /api/dashboard (projects[]._invoiceLines) - sales invoices (ACCREC).
  - Manual meta (expected date + comments) from /api/outstanding-invoices (shared store
    invoice:meta).
So it will now match Xero exactly like the Outstanding Invoices page does.

CHANGES
-------
1) SALES INVOICES ONLY - no more costs. Same source/API/data location as Outstanding
   Invoices.
2) GRAPH KEPT - "Invoices falling due by month" (green), now follows the filters.
3) FULL WIDTH page.
4) COLUMNS from your screenshot: INV No, Ref, To (customer), Date, Due date, Overdue
   (days late / days left), Expected (editable date - saved to shared meta), Paid, Due,
   Comments.
5) COMMENTS: click the Comments button on any row to VIEW and ADD comments (with @Name
   mentions). These are the SAME comments as the Outstanding Invoices / Invoices Owed
   commercial page - they read and write the shared invoice:meta store, so a comment
   added here shows there and vice versa. You can also delete comments.
6) Outstanding/All toggle + date range (default ~12 months, 2 ahead) + sortable columns.

FILE (single EDIT - no new files)
---------------------------------
  - pages/business-financials/invoices.js

DEPLOY
------
Replace the file. Commit -> Vercel "Ready" -> hard-refresh. No sync button here - it
reads the same live dashboard data the Outstanding Invoices page uses (sync invoices
from that page or the existing invoice sync as before).

VERIFY
------
  - Only sales invoices (INV- numbers, customers as "To"); totals match Xero like the
    Outstanding Invoices page.
  - Columns match your screenshot; Expected date is editable and persists.
  - Comments button opens a pop-out; adding a comment here appears on the Outstanding
    Invoices page too.
  - Page is full width; graph retained.

NOTE
----
The "Expected" and comments are stored per invoice NUMBER, shared with the other page.
If an invoice has no number (rare), its expected/comments can't be keyed - tell me if
you hit that and I'll add a fallback key.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
