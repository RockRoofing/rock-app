DEPLOY NOTES - Warranties page (flat thumbnails, customer-visible)
==================================================================

THREE files. TWO are NEW (add via GitHub "Add file"), ONE is an EDIT.

  NEW   pages/api/design-warranties.js
  NEW   pages/api/design-warranties-zip.js
  EDIT  pages/design/[project]/warranties.js   (was the 3-line shared page - now full page)

*** Create the two NEW files via "Add file" at those exact paths. ***

WHAT THIS IS
  Warranties now works like Handover Docs BUT with NO sections - just a simple flat page:
  - "+ Upload Warranties" (Rock Roofing only) - upload one or many documents; they sit as
    THUMBNAILS (image preview or PDF first-page preview).
  - Each thumbnail has View (in-page viewer, does not lose your selection), Download, and
    (Rock Roofing only) Delete.
  - Tick boxes + "Download all" / "Download selected" as a single ZIP.

  VISIBLE TO CUSTOMERS: customers can view and download warranties (and select + zip). They
  cannot upload or delete.

NOTES
  - Fresh store - warranties previously uploaded via the old shared page are not migrated;
    re-upload via "+ Upload Warranties".
  - No cron / notifications for this page (warranties are just documents to view/download).

BUILD: next build -> Compiled successfully.
