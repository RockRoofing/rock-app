Financial App — Outstanding Invoices: mention email link + live highlighted mentions
====================================================================================
Deploy target: RockRoofing/rock-app  branch main
Build: npm run build -> ✓ Compiled successfully
Replaces the outstanding-invoices files (deploy over the top).

1) LINK IN THE MENTION EMAIL
----------------------------
The "You were mentioned on invoice X" email now includes an "Open Outstanding
Invoices" button that links straight to the Outstanding Invoices page in the
portal.

2) @MENTION HIGHLIGHTED WHILE TYPING
------------------------------------
When you insert an @Name in the comment box, it now shows bold + blue in the
compose box itself (not just after posting), so it's clear the person has been
selected. (Implemented with a highlight layer behind the input; caret and typing
behave normally.)

FILES CHANGED
-------------
• pages/api/outstanding-invoices.js — link button in the mention email
• pages/outstanding-invoices.js     — live bold-blue @mentions in the compose box
