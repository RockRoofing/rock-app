DEPLOY NOTES - Form builder: "Your name (auto)" block
=====================================================

TWO files, both EDITS - just replace them.
  pages/operations/forms-builder.js   (new field type)
  pages/forms/fill.js                 (auto-fills + renders it)

WHAT'S NEW
  New block "Your name (auto - logged-in user)". When someone fills the form it
  automatically shows and records the name of the Site App user who is logged in -
  read-only, no typing needed. Add it in the form builder from the field-type list.

BUILD: next build -> Compiled successfully.
