DEPLOY NOTES - H&S Matrix: bigger attachment icon + red dot when missing
========================================================================

ONE file, EDIT. Replace in GitHub.

  EDIT  pages/operations/hs/hs-matrix.js

WHAT CHANGED
- The certificate paperclip icon on each cell is now noticeably BIGGER and easier to tap.
- Any cell that HAS a training date/expiry but NO attached certificate now shows a small RED DOT
  in the corner - so you can see at a glance which entries are still missing their certificate.
- Empty cells (no date at all) show nothing, as before.

NO vercel.json change. No cron. BUILD: Compiled successfully.
