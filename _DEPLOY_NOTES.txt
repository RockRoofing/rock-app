DEPLOY NOTES - Superseded items show N/A (not "To Be Approved")
===============================================================

THREE files, all EDITS - just replace them.
  pages/design/[project]/tech-sub.js
  pages/design/[project]/rock-drawings.js
  pages/design/[project]/calculations.js

CHANGE
  A superseded revision is no longer shown as needing approval. Instead of "To Be Approved"
  / "In Review", a superseded item now shows a grey "N/A".
    - Tech Sub: the Approval column shows N/A for superseded rows (and the viewer badge).
    - Rock Drawings & Calculations: the viewer badge for a superseded revision shows N/A.
  If a revision was actually approved before being superseded, it still shows "Approved".

  (The top approval banner already ignored superseded items, so a superseded item never
  triggers "APPROVALS NEEDED".)

BUILD: next build -> Compiled successfully.
