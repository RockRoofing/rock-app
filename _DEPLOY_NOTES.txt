Materials picker — add PO lines one at a time (fix)
===================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully  ·  Applications tab shows "· v11"

FIX
---
Adding a single line from a PO no longer collapses/hides the whole PO. A PO now
only collapses when ALL of its lines have been added.
• Add lines one at a time — each added line turns green with "✓ Added" while the
  rest stay visible and addable.
• The PO header shows "N/total added" while partially added; the top button
  becomes "Add remaining" (adds only the not-yet-added lines), and "Added / All
  added" once every line is in.
• Individual line still pulls supplier + PO number (group header) and the rate.

FILES
-----
• pages/applications.js   (v11)
