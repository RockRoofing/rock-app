DEPLOY NOTES - Invoice Finance: orange import button + sync button
==================================================================

TWO files, both EDITS - just replace them.
  components/BizNav.js                          (provides SyncButton - safe to re-copy)
  pages/business-financials/invoice-finance.js  (orange import + sync button)

CHANGES
  1. The "Import Bibby limit list (CSV)" button is now orange/gold (matching the
     other action buttons), white text.
  2. A "Sync invoices from Xero" button added to the page header - pulls fresh sales
     invoices (same /api/sync-invoices) and reloads the debtor list.

BizNav.js is included because the sync button uses the shared SyncButton component;
re-copying it is harmless if you already deployed it.

BUILD: next build -> Compiled successfully.
