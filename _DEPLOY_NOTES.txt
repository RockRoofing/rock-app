pkg787 - Retention Owed = Applied for x Ret %. Nothing else.
=============================================================

DEPLOY OVER pkg786. Thirteen files. Contains pkg786.

FOLDER NOTE: pages/project/[id].js uses LITERAL square brackets.

REPLACE:
  pages/retention.js                (the fix)
  everything else as pkg786

WHY THE LAST TWO ATTEMPTS FAILED
---------------------------------
A row is built in TWO places:

  line 433  project-derived rows      retentionOwed from p.totalRetention
  line 558  saved entries merged      retentionOwed from x.retentionOwed

Both took it from the dashboard. I patched the SECOND one twice. J109 is built
by the FIRST, so nothing I did could ever have reached it. You were right that
it should not have been difficult - I was fixing a path the row does not use.

NOW
---
One function, calcRetentionOwed(entry), and it is exactly what you said:

    Applied for  x  Ret %

Nothing external. Both figures are already resolved and already on screen by the
time the row renders, so the three cells can be read straight across.

Used by all four consumers - the cell, the sort, the outstanding card and the
totals row - so neither construction path matters any more and they cannot
disagree.

  J109   £112,263.79 x 5%   =   £5,613.19

Handles whole numbers (5) and fractions (0.05), and returns zero only when one
of the two columns is genuinely empty.

WHAT THIS CHANGES ELSEWHERE
----------------------------
The retentionClaimed deduction is gone from this column - it was part of the old
dashboard figure. Retention Owed is now the gross retention on what has been
applied for. Halves already released are still deducted in the CARD, which is
where you asked for them.

The tooltip now reads: "Applied for x Ret % - exactly the two columns to the
left. Nothing else feeds it."
