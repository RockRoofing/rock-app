DEPLOY NOTES - Budgets forecast dropdown: clean labels + holds "Budget / mo"
============================================================================

** Supersedes "Budget_forecast_option". One file, an EDIT - just replace it. **
  pages/business-financials/budgets.js

CHANGES
  1. Removed the "1 - / 2 - / 3 -" number prefixes from the Forecast dropdown.
     Options now read: Avg this FY | Last 3 mo | Last full month |
     Make same as Budget / mo

  2. When you pick "Make same as Budget / mo", the dropdown now HOLDS that label
     (previously it snapped back). It still fills the remaining months of the FY
     with the Budget / Mo figure, and now also remembers the choice on the row.

  Notes:
  - Filled months are stored as overrides, so future cells stay at the budget figure
    even if you later tweak the monthly Budget. To re-apply a NEW budget figure to
    the rest of the year, just pick the option again.
  - Completed months keep their actuals. Change is saved immediately.

BUILD: next build -> Compiled successfully.
