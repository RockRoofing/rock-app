GANTT — copy auto-deselect + fix cell selecting the row above

pages/operations/project-management/planning.js

1) COPY NOW AUTO-DESELECTS
   - After you click Copy, the copied cells are deselected automatically, so you can click straight into
     the paste target. (Flow: select gang -> Copy -> click target start cell -> Paste.)

2) CLICKING A CELL NO LONGER SELECTS THE ROW ABOVE
   - Cause: the yellow selection bar appeared IN the page flow, pushing the gantt down as it appeared — so
     by the time the click registered, your cursor was over the row above.
   - Fix: the yellow bar is now a FIXED floating overlay pinned to the top-centre of the screen. Showing/
     hiding it no longer shifts the gantt at all, so the cell you click is the cell that gets selected. It
     still stays visible while you scroll (as before).

NOTE: this file supersedes all previous gantt drops — deploy just this one.

Deploy this file. No cron/env change.
