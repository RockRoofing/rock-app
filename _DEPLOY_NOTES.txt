DEPLOY NOTES - Tech Sub approval: customer-only + digital record
================================================================

TWO files, both EDITS - just replace them.
  pages/api/design-techsubs.js
  pages/design/[project]/tech-sub.js

CHANGES
1. SUPERSEDED tech subs can no longer be approved - the Approve button is hidden and the
   server rejects it.
2. Only a CUSTOMER (the assigned approver) can approve. The Approve button is no longer
   shown to Rock Roofing team members at all, and the server blocks internal approvals.
3. DIGITAL APPROVAL RECORD: once approved, a date/time-stamped record is shown at the
   bottom of the tech sub in the viewer - who approved (name), their company, role, email,
   phone, the revision, and the full date + time of approval (UK time).

NOTE ON THE RECORD FIELDS
  Name, email, company and phone come from the customer's Design-portal account. There is
  currently no separate "job title" field on customer accounts, so Role shows "Customer".
  If you want a proper job-title captured, I can add that field to the customer record in
  the Admin centre - just say so.

BUILD: next build -> Compiled successfully.
