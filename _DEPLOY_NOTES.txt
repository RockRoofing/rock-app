pkg408 - Scorecards before Sales Dashboard
==========================================

Deploy after pkg407. One changed file: components/PreContractNav.js. No new files.

The Pre-Contract nav now reads:

    [CRM|Pre-Contract]  <- Portal | Scorecards | Sales Dashboard | Negotiating
    | Project Financials | Internal Handover Minutes | Handover Docs

Only those two swapped.

NOTE: the AreaToggle still sends "Pre-Contract" to /sales-crm, so clicking it from the CRM
lands on the Sales Dashboard rather than Scorecards - which is now the second tab, not the
first. If you would rather it landed on Scorecards, that is one line in
components/AreaToggle.js. Say the word.
