DEPLOY NOTES - Commercial Tasks: range, column width, % fix
===========================================================

ONE file, an EDIT - just replace it.
  pages/commercial-objectives.js

CHANGES
1. DEFAULT RANGE now 6 periods back -> 3 forward (weeks and months), no scroll.
   Filtering to a wider span still turns on scroll.
2. OBJECTIVE COLUMN is wider (420px) and each question is clamped to a max of 3 lines
   for legibility (full text on hover).
3. COMPLETED % FIX. It was wrongly counting future periods. It now counts ONLY:
   - from 30 Jul 2026 (when tracking began) or 6 months ago, whichever is later,
   - up to and including the current period,
   - never future periods / days that haven't happened yet.
   Box relabelled "Completed (last 6 months)". Yes = 1; No or blank = 0.

BUILD: next build -> Compiled successfully.
