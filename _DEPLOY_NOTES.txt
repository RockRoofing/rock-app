pkg850 - dashboard.js (step 7 of 8)
===================================

ONE FILE.  pages/api/dashboard.js

On its own deliberately. This is the single most-read file in the app - it
feeds Project Financials, the Budget Tracker, Retention, WIP, the CRM GP card
and the 13-week cash flow. If a number moves after this, there is exactly one
file it can be.

WHAT SHOULD LOOK DIFFERENT AFTER DEPLOYING THIS:  NOTHING.


REQUIRES pkg843 (lib/db.js) TO BE LIVE.


NEW FILES TO CREATE BY HAND IN GITHUB
-------------------------------------
None.

Running total: 47 of 49 converted. TWO REMAIN (pkg851):
  pages/api/business-financials.js  1694 lines
  pages/api/cron/deep-sync.js        399 lines


WHAT CHANGED
------------
Three edits. Nothing else in 1075 lines moved.

  1. getClient added to the existing lib/db import on line 1
  2. the local getRedis() helper deleted
  3. one line: const redis = await getRedis()  ->  await getClient()

The "if (!redis) return 500" guard went with it, because getClient() cannot
return null.

dashboard.js passes the client into syncRegistry() from lib/projectRegistry.js.
Checked: that uses only .set. The file itself uses only .get (13 times) and
.set (once). All on the shared client.


THE 12 KEYS IT TOUCHES, all verified unchanged
----------------------------------------------
  comment:{id}                 invoiced:lines:{id}
  costs:latest:{id}            invoiced:lines:__UNASSIGNED__
  costs:lines:{id}             ops:projects
  dashboard:cache              retention:entries
  invoiced:latest:{id}         wip:adjustments:{id}
  wip:latest:{id}              wip:adjustments:{p.xeroId}

Listed in full because a mistyped key here would not error - it would return
nothing, a total would quietly become zero, and the page would still render.
That is the whole reason for the baseline sheet.


CHECKS I RAN BEFORE PACKAGING
-----------------------------
- esbuild compile: 0 errors
- all 12 key strings extracted and diffed old vs new: identical, none lost,
  none added
- full text diff read: the three edits above and nothing else
- every redis operation used (.get, .set) confirmed present on the shared client
- syncRegistry(), the only helper handed the client, checked for its operations


WHAT TO CLICK AFTER DEPLOYING
-----------------------------
This is the one to go through the whole baseline sheet for. Nearly every row
on it comes through this file.

Priority order:
1. Project Financials - project count, and gross margin and total costs on
   your largest live project.
2. Retention - 612, Applied For, Certified.
3. WIP - Total WIP and Profit in WIP.
4. 13-week cash flow - closing balance, final week. Check the build marker
   changed first.
5. CRM GP margin card - and open the drill-down, the totals row should still
   match the card (that was pkg846).
6. Outstanding Invoices - Total due (unpaid).
7. Operations - live project count.

If a figure has moved, tell me which one and what it was. With one file in the
package the cause is findable quickly.
