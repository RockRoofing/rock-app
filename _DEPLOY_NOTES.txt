PLANNING — PHASE 1 of 5: Operatives roster (H&S)

  pages/api/operatives.js              New roster store (ops:operatives-roster). Separate from Site App
                                       login users. GET/POST/DELETE.
  pages/operations/hs/operatives.js    Replaces the coming-soon stub. Table with:
                                       - Add new (top), sortable headers (Name/Email/Phone/Company/Trade)
                                       - Search by name, filter by Trade and Company, live count
                                       - Add/edit modal: First Name*, Last Name*, Email, Phone, Company,
                                         Trade (multiselect: Single Ply, Felt, Liquids, Hot Melt, Rainscreen,
                                         Composite Panels, Aluminium, Standing Seam, Labourer, Other)

This is the installer roster that will feed the Planning Gantt (Phase 3). Deploy and add your operatives
so they're ready to allocate. The nav entry (H&S → Operatives) already exists.

NEXT PHASES (coming): 2) Planning data model + API (allocations, clash rules, project start/completion
dates); 3) Gantt UI; 4) Weekly pop-out (PDF + per-operative email); 5) Forms split (Completed | Missing).
