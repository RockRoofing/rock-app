DEPLOY NOTES - Sales table still empty: on-page diagnostic to pinpoint it
========================================================================

I'm no longer guessing - this build PRINTS the diagnosis on the page.

WHAT TO DO
----------
1. Deploy all three files. Hard-refresh the Sales page.
2. Click "Sync Xero figures" and wait for it to finish.
3. If the table is still empty, a grey monospace line appears where the table would be,
   e.g.:
     diag - chart codes: {"201":123456} | ledger codes: [] | ledger lines: 0 | requested: ["200","201"]
   COPY THAT LINE TO ME. It tells us exactly:
     - chart codes  = the account code(s) your sales figure sits under (from the P&L)
     - ledger codes = what the sales ledger actually captured
     - requested    = the codes the sync tried to pull
   From that I can see precisely why they don't line up and fix it in one go.

WHAT THIS BUILD ALREADY TRIES
-----------------------------
The sync now pulls the general ledger for code 200, any code tagged 'sales', AND any
code the P&L puts in the INCOME section (whatever its number). The chart and ledger use
the same code set. codeSection flows P&L -> benchmark -> ledger, so ONE sync should
populate it. If it still comes up empty, the diag line will show whether:
  (a) the ledger genuinely returned no lines for those codes (a Xero/journal issue), or
  (b) the codes still don't match (config issue) - either way the numbers make it clear.

LIKELY culprits the diag will confirm:
  - Your sales P&L line maps to a code other than 200 AND the benchmark wasn't re-synced
    with codeSection yet (this sync fixes that).
  - Sales are posted in a way the Journals endpoint reports under a code we're not
    requesting.

FILES (all EDITS - no new files)
--------------------------------
  - pages/business-financials/sales.js        (on-page diag when table empty)
  - pages/api/business-financials.js           (diag payload + income-section codes)
  - pages/api/sync-benchmark.js                (ledger pulls income-section codes)

DEPLOY
------
Replace all three. Commit -> Vercel "Ready" -> hard-refresh -> Sync Xero figures ->
if empty, send me the diag line.

BUILD
-----
Validated with `next build` -> "Compiled successfully".
