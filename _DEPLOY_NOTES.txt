DEPLOY NOTES - Forecast application: 100% ticks
===============================================

ONE file, an EDIT - just replace it.
  pages/project-cashflow.js

CHANGE
  In the forecasted application, the contract works table now has:
    - A per-line "100%" tick - tick to set that line to 100% complete (untick -> 0%).
    - An "All lines 100%" tick above the table - sets every works line to 100% at once
      (untick -> 0%). It shows ticked when all lines are already at 100%.
  The % complete box still works for anything in between.

BUILD: next build -> Compiled successfully.
