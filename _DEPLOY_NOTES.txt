DEPLOY NOTES - Fix invalid_scope (remove finance.statements.read)
=================================================================

The invalid_scope error was caused by finance.statements.read, which I added as a
fallback - that's a separate Xero Finance API product your app isn't enabled for, and it
wasn't in your authorised scopes list. Removed it.

Kept accounting.reports.taxreports.read (which IS in your app's authorised list, so it
won't trip invalid_scope).

DEPLOY
------
Replace pages/connect.js. Commit -> Vercel "Ready" -> go to /connect and reconnect Xero.
The consent screen should now load and ask you to approve the reports permission.

Then open VAT Refund and send me the diag line as before.

FILE
----
  - pages/connect.js

BUILD: next build -> Compiled successfully.
