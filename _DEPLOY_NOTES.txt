pkg276 - Site App report now matches the portal
================================================

NOTE: pkg276 REPLACES pkg272, 273, 274 AND 275. Same file list, with the Site App report
form brought up to match the portal. Deploy this one and ignore the earlier four.


CHANGED FILES
-------------
  pages/forms/index.js                    Site App report: attachments + screenshot paste
  components/OperationsNav.js             top navigation 20px + sub-tabs 15px
  middleware.js                           Site App report fix
  components/ReportProblemButton.js       portal attachments + screenshot paste
  pages/api/report-problem.js             store attachments, email them
  pages/admin/problem-reports.js          show attachments

No new files. All are straight replacements.


WHAT I CHANGED - AND WHAT I DID NOT
------------------------------------
I read "the exact same as the portal" as the REPORT ITSELF - what it can do - because the
one real difference left was that the portal took attachments and pasted screenshots and
the Site App did not. That gap is now closed.

I did NOT change the trigger link's appearance. You specifically asked last time for the
Site App link to be orange, slightly bigger and NOT a button, and it still is (orange
#ea580c, 15.5px, underlined). The portal's is gold 13px, so making them identical would
have undone what you just asked for.

If you actually meant the LINK should look the same as the portal's, say so and it is a
one-line change.


SITE APP REPORT NOW HAS
------------------------
  "+ Add a photo or screenshot"   picks files - on a phone this offers the camera, the
                                  photo library or a file, multiple at once.
  Paste                           paste an image anywhere in the sheet and it uploads.
  Thumbnails                      each attachment shows with a Remove button before send.
  Send blocked mid-upload         so nothing goes half-finished.

These land in exactly the same place as portal ones: embedded inline in the office email,
and as thumbnails on Admin > App Improvements.

ONE DIFFERENCE ON PURPOSE: the Site App uploads via /api/upload-file with compressImage,
the same route Deliveries and Issues already use - NOT the blob client the portal uses.
Photos straight off a phone camera are large, and this compresses them first. The controls
are also sized for one-handed use (bigger tap targets) rather than copied pixel-for-pixel
from the desktop.


TESTED
------
npm run build - Compiled successfully.


STILL OUTSTANDING
-----------------
The zip I am working from is pkg264 and does not contain pkg265. Please confirm whether
pkg265 was deployed - if it was, send a fresh export before the next package.
