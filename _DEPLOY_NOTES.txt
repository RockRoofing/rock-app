SITE APP + PORTAL — fixes batch 2

DRAWINGS / RAMS VIEWER (pages/forms/index.js)
  - The FileViewer now renders through a React portal to document.body and stays a fixed full-screen
    overlay, so View no longer navigates away to the file URL — you stay in the app and can scroll
    left/right (arrows / Prev-Next / swipe / arrow keys) through the drawings. Download goes through the
    proxy (keeps filename, no navigation). Images try the raw URL first then the inline proxy.

DELIVERIES / ATTACHMENT VIEWER (components/RowAttachments.js)
  - The viewer now loads images from the raw blob URL first and only falls back to the inline proxy, then
    to a download panel — so correctly-uploaded images display in-app instead of showing "Download to view".
  (Note: photos uploaded before the earlier FormData upload fix are corrupted and will still not preview —
   re-upload those.)

VARIATIONS (pages/forms/cm/variations.js)
  - Now reads variations from /api/dashboard (matching by projectNo, forcing a sync if the cache lacks the
    project) — this is where variation data actually lives — so the table populates. Still project-first,
    then Variation number / Description / Instructed / Total.

SRATs (pages/forms/cm/srats.js)
  - "Responsible" on added tasks is now a dropdown of Portal Users (from /api/team), not a free text box.

ISSUE NOTIFICATION EMAILS (pages/api/issue-notify.js)
  - Only the Contracts Manager now gets the "Review & action this issue" button. The Operations Manager and
    QS get an information-only email (same details + photo attachments, no action button).

LIVE PROJECT TASKS
  - pages/operations/project-management/tasks.js: new "Date Added" column (auto from createdAt) in the
    portal table.
  - pages/forms/cm/tasks.js: shows "Added <date>" on each card; "Responsible" is now a Portal Users
    dropdown; Target Completion colour fixed — on/after today = GREEN, past due = RED (was all red).
  - Home overdue badge counts strictly past-due tasks.

Deploy all files. No cron/env change.

STILL TO COME (next drop):
  - Pre-Start area rework (project-first; Complete new vs View completed; Submit & send to customer with
    IHM customer contacts add/delete).
  - Forms: view/edit completed forms from inside the Site App.
