DELIVERIES — View button fix + Scheduled Delivery green rule

  components/RowAttachments.js
   - Fix: clicking "View" now closes the attachments list modal and opens the full-screen viewer cleanly
     (previously the viewer could open behind the still-open modal and appear to do nothing).
   - Viewer is now robust to files with no extension / missing type: it attempts to render as an image and
     only falls back to the "Download to view" panel if the image genuinely fails to load. PDFs still embed.
   (Affects Deliveries, Procurement, Risk Log, Live Tasks — all share this component.)

  pages/operations/project-management/deliveries.js
   - The Scheduled Delivery cell now goes green ONLY when a scheduled date is actually set. Marking
     "PO Sent" = Yes no longer turns the Scheduled Delivery cell green. (PO Sent still greens its own cell
     and the columns to its left as before.)

Deploy both. No cron/env change.
