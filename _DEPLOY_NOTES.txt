DEPLOY NOTES - Summary page update (re-cut, current versions)
=============================================================

TWO files, both EDITS - just replace them.
  pages/business-financials.js       (the Summary page)
  pages/api/business-financials.js   (API)

WHY RE-CUT
  This is the Summary page update you hadn't deployed yet. Because you've deployed
  everything else since, these are the CURRENT versions of both files - they include
  the summary changes AND all the later API changes (project cash-flow forecasts,
  12-month lumps, etc.), so deploying them will NOT revert anything.

WHAT'S IN IT (Summary page)
  - KPI cards (current FY, year to date): Gross margin, Net margin, Overheads % of
    revenue, and Monthly overheads (last full month).
  - Sales - 2 years line chart with trend line + a monthly sales bar chart.
  - Gross margin and Net margin line charts, each with a trend line.
  - Cost of sale by category pie relabelled (real Labour / Materials / Overheads spend).
  API adds netMarginPct + overheadPct to the monthly series (used by the cards/charts).

BUILD: next build -> Compiled successfully.
