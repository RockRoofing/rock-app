SRATs PAGE + PROJECT CONCERNS SORTABLE

DEPLOY (mirror into repo, overwrite):
  components/ProjectConcerns.js                        Project Concerns table now sortable by column headers
  pages/api/srats.js                                   NEW — SRATs storage
  pages/operations/project-management/srat.js          NEW — SRATs page (replaces the coming-soon stub)

SRATs PAGE
  - Table: Project (No + Name), Situation, Roadblocks, Actions, Timeline. Sortable headers. "+ Add new".
  - Add/Edit modal (large): project dropdown (auto-fills name), Situation, Roadblocks,
    Actions (a text box PLUS a Live Project Tasks list with "+ Add new"; tasks can be marked
    resolved immediately). Timeline text box.
  - Actions tasks push ONE-WAY into Live Project Tasks (created there); removing from the SRAT
    just unlinks (leaves the Live Task in place). No live two-way sync.
  - View opens a large read-only view of the full SRAT.
