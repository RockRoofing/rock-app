DEPLOY NOTES - Handover Docs: in-page View button
==================================================

ONE file, an EDIT - just replace it.
  pages/design/[project]/handover-docs.js

CHANGES
  - Each thumbnail now has a "View" button (and clicking the thumbnail image also views).
  - View opens the document in an in-page viewer overlay (images, and multi-page PDFs you
    can scroll through) - it does NOT open a new tab or navigate away.
  - Because the viewer is on the same page, any documents you have TICKED stay selected -
    opening/closing the viewer never clears your selection.
  - Download and Delete remain on each thumbnail as before.

BUILD: next build -> Compiled successfully.
