Forms — Missing: show N/A (grey) when no forms are required
===========================================================
Deploy target: RockRoofing/rock-app  branch main
Build: ✓ Compiled successfully
Single file: pages/operations/forms-missing.js

CHANGE
------
When a form type needs no forms for the period (0/0), it previously showed a green
"100%". It now shows "N/A" in light grey instead. Same for the overall Completed
summary card — if nothing is required across the period, it shows N/A rather than
100%. The "x/y done" line is unchanged.

FILES
-----
• pages/operations/forms-missing.js
